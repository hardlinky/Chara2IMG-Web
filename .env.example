INVITE_SECRET=replace-with-invite-secret
COOKIE_SECRET=replace-with-cookie-signing-secret
# Optional for same-origin deployments; set only when the frontend runs from a different origin.
ALLOWED_ORIGIN=
PORT=3000
