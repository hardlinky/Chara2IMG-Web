---
phase: 01-access-and-proxy-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/client/main.tsx
  - src/client/App.tsx
  - src/client/features/access/InviteGate.tsx
  - src/client/features/access/RunpodKeySettings.tsx
  - src/client/lib/runpodKeyStorage.ts
  - src/server/index.ts
  - src/server/middleware/security.ts
  - src/server/middleware/session.ts
  - src/server/routes/access.ts
  - src/server/schemas/access.ts
  - src/server/security/invite.ts
  - .env.example
autonomous: true
must_haves:
  truths:
    - User can access the app only through an invited-user path.
    - User can enter a Runpod API key in the app.
    - User can opt in to remembering the Runpod API key on the current browser.
    - A remembered API key is restored only when the same browser/device has prior opt-in.
  artifacts:
    - path: src/server/routes/access.ts
      provides: Invite verification, session check, and logout endpoints.
      exports: [registerAccessRoutes]
    - path: src/server/middleware/session.ts
      provides: Signed HttpOnly cookie session validation and session requirement middleware.
      exports: [issueSessionCookie, clearSessionCookie, requireInvitedSession]
    - path: src/client/features/access/InviteGate.tsx
      provides: Invite entry flow that blocks app access until session is valid.
    - path: src/client/features/access/RunpodKeySettings.tsx
      provides: BYOK capture UI with explicit remember toggle and clear action.
    - path: src/client/lib/runpodKeyStorage.ts
      provides: Browser-only storage strategy (memory default + optional local persistence).
      exports: [getRunpodKey, setRunpodKey, clearRunpodKey]
  key_links:
    - from: src/client/features/access/InviteGate.tsx
      to: /api/access/verify-invite
      via: POST request to create invited session
      pattern: /api/access/verify-invite
    - from: src/server/routes/access.ts
      to: src/server/middleware/session.ts
      via: Session cookie issue/check/clear helpers
      pattern: issueSessionCookie|requireInvitedSession|clearSessionCookie
    - from: src/client/features/access/RunpodKeySettings.tsx
      to: src/client/lib/runpodKeyStorage.ts
      via: Save/restore/clear key behavior tied to remember option
      pattern: getRunpodKey|setRunpodKey|clearRunpodKey
---

<objective>
Build the invited-user access boundary and BYOK settings flow for Phase 1.

Purpose: Establish secure app entry and browser-local key handling before any Runpod forwarding logic.
Output: Working invited session gate plus Runpod API key capture/remember UX with explicit same-browser persistence behavior.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold minimal React + Hono workspace for access-boundary work</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/client/main.tsx, src/client/App.tsx, src/server/index.ts, .env.example</files>
  <action>Create a minimal TypeScript workspace with React client and Hono server entrypoints that can run in development and build for production. Add scripts for `dev`, `build`, and `typecheck`. Add env placeholders for invite and cookie secrets plus allowed origin. Keep scope limited to infrastructure required for AUTH-01..AUTH-04 and avoid introducing generalized auth providers or database persistence.</action>
  <verify>`npm install`; `npm run typecheck`; `npm run build`</verify>
  <done>Client and server compile successfully, and the repository has a runnable baseline for access + BYOK features without any server-side key vault.</done>
</task>

<task type="auto">
  <name>Task 2: Implement invited-user session endpoints with secure middleware defaults</name>
  <files>src/server/middleware/security.ts, src/server/middleware/session.ts, src/server/security/invite.ts, src/server/schemas/access.ts, src/server/routes/access.ts, src/server/index.ts</files>
  <action>Implement `/api/access/verify-invite`, `/api/access/session`, and `/api/access/logout` routes using Hono and Zod. Validate invite input with strict schema, compare invite secrets in constant-time, and issue signed HttpOnly session cookies with secure attributes. Apply CORS, CSRF, and secure-header middleware for API routes. Explicitly avoid storing invite/session state in long-term server storage; only signed-cookie session is allowed in this phase.</action>
  <verify>`npm run typecheck`; start dev server and call `POST /api/access/verify-invite` with valid/invalid invite; call `GET /api/access/session` before and after login to confirm session state; call `POST /api/access/logout` to clear cookie.</verify>
  <done>Invited access is enforced by session checks, invalid invites are rejected, valid invites create secure cookie sessions, and logout clears access cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Build BYOK settings UI with explicit remember-on-this-browser behavior</name>
  <files>src/client/features/access/InviteGate.tsx, src/client/features/access/RunpodKeySettings.tsx, src/client/lib/runpodKeyStorage.ts, src/client/App.tsx</files>
  <action>Add an invite gate view that blocks app usage until `/api/access/session` confirms invited access, and a Runpod key settings panel where user can enter key and opt into remember-on-this-browser persistence. Default to non-persistent in-memory key handling. Persist to local storage only when user opts in, restore only from same browser/device local storage, and provide clear/remove action. Do not send key to server in this plan except where needed for future proxied calls.</action>
  <verify>`npm run build`; run app and verify: invite required before app access, key entry works, remember-off does not survive reload, remember-on survives reload on same browser, clear removes key.</verify>
  <done>User can complete invited entry and configure BYOK with explicit local remember semantics matching AUTH-01, AUTH-02, and AUTH-04.</done>
</task>

</tasks>

<verification>
Run end-to-end manual smoke checks for invited session lifecycle and BYOK remember behavior:
1. Access blocked before invite verification.
2. Valid invite grants session; invalid invite is denied.
3. BYOK value is available in-app after save.
4. Remember-off key is not restored after full reload.
5. Remember-on key is restored after full reload on same browser.
</verification>

<success_criteria>
- AUTH-01 satisfied: user can provide Runpod API key through UI.
- AUTH-02 satisfied: explicit browser-local remember option exists and behaves correctly.
- AUTH-04 satisfied: invited-user session gate prevents public open use.
- Access + key flows are implemented without long-term server-side secret persistence.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-01-SUMMARY.md`
</output>
