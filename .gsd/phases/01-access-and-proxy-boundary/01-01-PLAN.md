---
phase: 01-access-and-proxy-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/server/index.ts
  - src/server/middleware/security.ts
  - src/server/middleware/session.ts
  - src/server/routes/access.ts
  - src/server/schemas/access.ts
  - src/server/security/invite.ts
  - .env.example
autonomous: true
must_haves:
  truths:
    - User can access the app only through an invited-user path.
    - Invalid invite attempts are rejected without creating session state.
    - Valid invite verification creates an invited session that can be checked and revoked.
  artifacts:
    - path: src/server/routes/access.ts
      provides: Invite verification, session check, and logout endpoints.
      exports: [registerAccessRoutes]
    - path: src/server/middleware/session.ts
      provides: Signed HttpOnly cookie session validation and session requirement middleware.
      exports: [issueSessionCookie, clearSessionCookie, requireInvitedSession]
    - path: src/server/security/invite.ts
      provides: Constant-time invite secret comparison and validation helpers.
      exports: [verifyInviteSecret]
  key_links:
    - from: src/server/routes/access.ts
      to: src/server/middleware/session.ts
      via: Session cookie issue/check/clear helpers
      pattern: issueSessionCookie|requireInvitedSession|clearSessionCookie
    - from: src/server/routes/access.ts
      to: src/server/security/invite.ts
      via: Invite verification route uses constant-time compare helper
      pattern: verifyInviteSecret
---

<objective>
Build the server-side invited-user access boundary for Phase 1.

Purpose: Establish secure app entry semantics before client BYOK UX and proxy forwarding are layered on top.
Output: Runnable workspace baseline plus invite verification, session check, and logout endpoints with secure cookie middleware.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold minimal TypeScript + Hono baseline for access-boundary work</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/server/index.ts, .env.example</files>
  <action>Create a minimal TypeScript workspace with a Hono server entrypoint that can run in development and build for production, while remaining compatible with a Vite React client added in the next plan. Add scripts for `dev`, `build`, and `typecheck`. Add env placeholders for invite and cookie secrets plus allowed origin. Keep scope limited to infrastructure required for AUTH-04 and avoid introducing generalized auth providers or database persistence.</action>
  <verify>`npm install`; `npm run typecheck`; `npm run build`</verify>
  <done>Workspace compiles successfully with server baseline and env placeholders needed for invited-session access control.</done>
</task>

<task type="auto">
  <name>Task 2: Implement invited-user session endpoints with secure middleware defaults</name>
  <files>src/server/middleware/security.ts, src/server/middleware/session.ts, src/server/security/invite.ts, src/server/schemas/access.ts, src/server/routes/access.ts, src/server/index.ts</files>
  <action>Implement `/api/access/verify-invite`, `/api/access/session`, and `/api/access/logout` routes using Hono and Zod. Validate invite input with strict schema, compare invite secrets in constant-time, and issue signed HttpOnly session cookies with secure attributes. Apply CORS, CSRF, and secure-header middleware for API routes. Explicitly avoid storing invite/session state in long-term server storage; only signed-cookie session is allowed in this phase.</action>
  <verify>`npm run typecheck`; start dev server and call `POST /api/access/verify-invite` with valid/invalid invite; call `GET /api/access/session` before and after login to confirm session state; call `POST /api/access/logout` to clear cookie.</verify>
  <done>Invited access is enforced by session checks, invalid invites are rejected, valid invites create secure cookie sessions, and logout clears access cleanly.</done>
</task>

</tasks>

<verification>
Run server-side smoke checks for invited session lifecycle:
1. `POST /api/access/verify-invite` rejects invalid invite.
2. Valid invite grants session cookie.
3. `GET /api/access/session` returns invited state after login.
4. `POST /api/access/logout` clears access state.
</verification>

<success_criteria>
- AUTH-04 backend boundary is in place: only invited session path grants access.
- Invite verification and cookie session lifecycle are secure and testable from API routes.
- No long-term server-side secret persistence is introduced.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-01-SUMMARY.md`
</output>
