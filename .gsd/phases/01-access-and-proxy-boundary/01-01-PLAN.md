---
phase: 01-access-and-proxy-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/server/index.ts
  - .env.example
autonomous: true
must_haves:
  truths:
    - Project boots with a minimal server baseline ready for invited-session implementation.
    - Environment placeholders exist for invite and cookie security configuration.
    - Build and typecheck pass before access/session logic is layered in.
  artifacts:
    - path: package.json
      provides: Dev/build/typecheck scripts and baseline dependencies for server-first work.
    - path: src/server/index.ts
      provides: Minimal Hono server entrypoint with health route and startup wiring.
    - path: .env.example
      provides: Invite/cookie/origin security placeholders required by later access boundary plans.
  key_links:
    - from: package.json
      to: src/server/index.ts
      via: Dev/build scripts execute the server entrypoint and compile cleanly
      pattern: dev|build|typecheck
    - from: .env.example
      to: src/server/index.ts
      via: Environment contract reserved for upcoming access/session middleware
      pattern: INVITE|COOKIE|ORIGIN
---

<objective>
Scaffold the minimal server and workspace baseline for Phase 1.

Purpose: Isolate baseline setup from auth/session implementation so execution stays within healthy context breadth.
Output: Runnable TypeScript + Hono baseline, core scripts, and env placeholders required by downstream access plans.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold minimal TypeScript + Hono baseline for Phase 1</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/server/index.ts, .env.example</files>
  <action>Create a minimal TypeScript workspace with a Hono server entrypoint that can run in development and build for production. Add scripts for `dev`, `build`, and `typecheck`. Add env placeholders for invite and cookie secrets plus allowed origin. Keep scope strictly to baseline infrastructure and do not introduce access/session middleware yet.</action>
  <verify>`npm install`; `npm run typecheck`; `npm run build`</verify>
  <done>Workspace compiles successfully with server baseline and environment contract ready for invited-session implementation.</done>
</task>

</tasks>

<verification>
Confirm baseline stability:
1. `npm run typecheck` passes.
2. `npm run build` passes.
3. Server entrypoint boots with configured environment placeholders.
</verification>

<success_criteria>
- Baseline workspace and server scaffold are runnable and build-clean.
- Environment contract for invite/session security is present.
- No auth/session logic is prematurely mixed into baseline scaffolding.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-01-SUMMARY.md`
</output>
