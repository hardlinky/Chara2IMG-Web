---
phase: 01-access-and-proxy-boundary
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/server/middleware/security.ts
  - src/server/middleware/session.ts
  - src/server/security/invite.ts
  - src/server/schemas/access.ts
  - src/server/routes/access.ts
  - src/server/index.ts
autonomous: true
must_haves:
  truths:
    - User can access the app only through an invited-user path.
    - Invalid invite attempts are rejected without creating session state.
    - Valid invite verification creates an invited session that can be checked and revoked.
  artifacts:
    - path: src/server/routes/access.ts
      provides: Invite verification, session check, and logout endpoints.
      exports: [registerAccessRoutes]
    - path: src/server/middleware/session.ts
      provides: Signed HttpOnly cookie session validation and session requirement middleware.
      exports: [issueSessionCookie, clearSessionCookie, requireInvitedSession]
    - path: src/server/security/invite.ts
      provides: Constant-time invite secret comparison and validation helpers.
      exports: [verifyInviteSecret]
  key_links:
    - from: src/server/routes/access.ts
      to: src/server/middleware/session.ts
      via: Session cookie issue/check/clear helpers
      pattern: issueSessionCookie|requireInvitedSession|clearSessionCookie
    - from: src/server/routes/access.ts
      to: src/server/security/invite.ts
      via: Invite verification route uses constant-time compare helper
      pattern: verifyInviteSecret
---

<objective>
Implement server-side invited-user session boundary and secure access middleware.

Purpose: Complete AUTH-04 boundary behavior on top of the baseline scaffold while keeping this concern isolated from client BYOK UX.
Output: Invite verification, invited-session check, and logout routes with secure cookie/session middleware.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement invited-session middleware primitives</name>
  <files>src/server/middleware/security.ts, src/server/middleware/session.ts, src/server/security/invite.ts, src/server/index.ts</files>
  <action>Implement CORS, CSRF, and secure-header middleware defaults for API routes. Add constant-time invite verification helper and signed HttpOnly cookie session helpers (issue, validate, clear). Keep all session handling cookie-based and avoid any long-term persistence layer.</action>
  <verify>`npm run typecheck`; start dev server and confirm middleware + session helper imports compile and attach cleanly.</verify>
  <done>Server contains reusable security and session middleware primitives required for invited-only access.</done>
</task>

<task type="auto">
  <name>Task 2: Add invite/session access routes wired to middleware</name>
  <files>src/server/routes/access.ts, src/server/schemas/access.ts, src/server/index.ts</files>
  <action>Implement `/api/access/verify-invite`, `/api/access/session`, and `/api/access/logout` using Hono and Zod. Validate invite input with strict schemas, reject malformed payloads, create invited session only for valid invite checks, and clear session on logout. Keep route behavior narrow to invited boundary semantics.</action>
  <verify>`npm run typecheck`; call `POST /api/access/verify-invite` with valid/invalid invite; call `GET /api/access/session` before/after login; call `POST /api/access/logout` and confirm session cleared.</verify>
  <done>Invited access is enforced end-to-end by secure middleware and dedicated access routes.</done>
</task>

</tasks>

<verification>
Run server-side smoke checks for invited session lifecycle:
1. `POST /api/access/verify-invite` rejects invalid invite.
2. Valid invite grants session cookie.
3. `GET /api/access/session` returns invited state after login.
4. `POST /api/access/logout` clears access state.
</verification>

<success_criteria>
- AUTH-04 backend boundary is in place: only invited session path grants access.
- Invite verification and cookie session lifecycle are secure and testable from API routes.
- No long-term server-side secret persistence is introduced.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-02-SUMMARY.md`
</output>
