---
phase: 01-access-and-proxy-boundary
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/client/main.tsx
  - src/client/App.tsx
  - src/client/features/access/InviteGate.tsx
  - src/client/features/access/RunpodKeySettings.tsx
  - src/client/lib/runpodKeyStorage.ts
autonomous: true
must_haves:
  truths:
    - User can provide a Runpod API key from the app UI.
    - User can opt in to remember the API key only on the current browser/device.
    - Key value is not restored after reload when remember is off.
    - Invited session check gates app access before BYOK settings are shown.
  artifacts:
    - path: src/client/features/access/InviteGate.tsx
      provides: Invite entry flow that blocks app access until session is valid.
    - path: src/client/features/access/RunpodKeySettings.tsx
      provides: BYOK capture UI with explicit remember toggle and clear action.
    - path: src/client/lib/runpodKeyStorage.ts
      provides: Browser-only storage strategy (memory default + optional local persistence).
      exports: [getRunpodKey, setRunpodKey, clearRunpodKey]
  key_links:
    - from: src/client/features/access/InviteGate.tsx
      to: /api/access/verify-invite
      via: POST request to create invited session
      pattern: /api/access/verify-invite
    - from: src/client/features/access/InviteGate.tsx
      to: /api/access/session
      via: Session gate check on app load
      pattern: /api/access/session
    - from: src/client/features/access/RunpodKeySettings.tsx
      to: src/client/lib/runpodKeyStorage.ts
      via: Save/restore/clear key behavior tied to remember option
      pattern: getRunpodKey|setRunpodKey|clearRunpodKey
---

<objective>
Implement the invited client gate and BYOK settings UX for Phase 1.

Purpose: Complete AUTH-01 and AUTH-02 while consuming the server invite/session boundary created in Plan 01-02.
Output: Invite-gated React app shell and Runpod key settings with explicit remember-on-this-browser behavior.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
@.gsd/phases/01-access-and-proxy-boundary/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build React app shell with invited-session gate wiring</name>
  <files>src/client/main.tsx, src/client/App.tsx, src/client/features/access/InviteGate.tsx</files>
  <action>Create the client entrypoint and app shell that blocks feature access until `/api/access/session` confirms invited state. Add invite submission flow that calls `/api/access/verify-invite`, handles invalid invite feedback, and refreshes session state after success. Keep UX minimal and focused on access gating; avoid introducing non-phase features.</action>
  <verify>`npm run build`; run app and confirm access is blocked before invite verification; invalid invite stays blocked; valid invite unlocks gated content.</verify>
  <done>Client app is invite-gated and correctly integrated with server access endpoints from Plan 01-02.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BYOK settings with explicit local remember semantics</name>
  <files>src/client/features/access/RunpodKeySettings.tsx, src/client/lib/runpodKeyStorage.ts, src/client/App.tsx</files>
  <action>Add Runpod key capture UI with explicit remember toggle and clear action. Default to non-persistent in-memory storage; persist to local storage only when remember is enabled. Restore remembered key only from same browser/device local storage. Do not send key to server in this plan except for future in-process use by later proxy client calls.</action>
  <verify>`npm run build`; manual checks confirm key save works, remember-off does not survive reload, remember-on survives reload on same browser, and clear removes stored value.</verify>
  <done>AUTH-01 and AUTH-02 are satisfied with explicit, browser-scoped BYOK behavior.</done>
</task>

</tasks>

<verification>
Confirm invited UX and BYOK behavior:
1. Access is blocked before invite verification.
2. Valid invite unlocks gated app shell; invalid invite does not.
3. User can enter and update Runpod API key in settings.
4. Remember-off key is not restored after full reload.
5. Remember-on key is restored after full reload on same browser/device.
</verification>

<success_criteria>
- AUTH-01 satisfied: user can provide Runpod API key through UI.
- AUTH-02 satisfied: explicit browser-local remember option exists and behaves correctly.
- Invited session gate from Plan 01-02 is reflected in client behavior.
- No long-term server-side key storage is introduced by client BYOK UX.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-03-SUMMARY.md`
</output>
