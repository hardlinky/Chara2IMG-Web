---
phase: 01-access-and-proxy-boundary
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/shared/contracts/runpod.ts
  - src/server/schemas/runpodProxy.ts
  - src/server/lib/runpodClient.ts
  - src/server/lib/redaction.ts
  - src/server/routes/runpodProxy.ts
  - src/server/index.ts
  - src/client/lib/api/runpodProxyClient.ts
  - src/client/features/access/RunpodProxySmoke.tsx
  - src/client/App.tsx
  - tests/server/runpodProxy.test.ts
autonomous: true
must_haves:
  truths:
    - Runpod lifecycle requests can be sent from the web app through backend proxy endpoints.
    - Proxy routes accept only the approved Runpod lifecycle operations needed by this product.
    - User-provided Runpod API key is forwarded per request without long-term server-side persistence.
    - Proxy behavior rejects malformed or overbroad requests instead of acting as a generic relay.
  artifacts:
    - path: src/server/routes/runpodProxy.ts
      provides: Allowlisted proxy endpoints for run/status/cancel/retry/purge-queue.
      exports: [registerRunpodProxyRoutes]
    - path: src/server/schemas/runpodProxy.ts
      provides: Strict Zod request schemas for each proxied lifecycle route.
      exports: [runRequestSchema, statusRequestSchema, cancelRequestSchema, retryRequestSchema, purgeQueueRequestSchema]
    - path: src/server/lib/runpodClient.ts
      provides: Runpod fetch wrapper that injects Authorization header from request-scoped key.
      exports: [forwardRunpodRequest]
    - path: tests/server/runpodProxy.test.ts
      provides: Automated coverage for allowlist validation, request forwarding, and key-safe behavior.
  key_links:
    - from: src/client/lib/api/runpodProxyClient.ts
      to: /api/runpod/*
      via: Typed client helpers for lifecycle proxy calls
      pattern: /api/runpod
    - from: src/server/routes/runpodProxy.ts
      to: src/server/lib/runpodClient.ts
      via: Per-route validated payload forwarded to Runpod API
      pattern: forwardRunpodRequest
    - from: src/server/lib/runpodClient.ts
      to: https://api.runpod.ai/v2/{endpointId}
      via: Outbound lifecycle fetch with request-scoped bearer header
      pattern: Authorization: Bearer
---

<objective>
Implement the thin Runpod proxy boundary with strict validation and secret-safe forwarding.

Purpose: Complete Phase 1 AUTH-03 by enabling lifecycle forwarding through a safe backend boundary that does not become an open proxy or key vault.
Output: Validated allowlist proxy routes, typed client helpers, and tests confirming no long-term server key persistence behavior.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
@.gsd/phases/01-access-and-proxy-boundary/01-01-SUMMARY.md
@.gsd/phases/01-access-and-proxy-boundary/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build strict allowlist Runpod proxy contract and server routes</name>
  <files>src/shared/contracts/runpod.ts, src/server/schemas/runpodProxy.ts, src/server/routes/runpodProxy.ts, src/server/index.ts</files>
  <action>Create explicit proxy endpoints for the approved lifecycle operations used by this app: run, status, cancel, retry, and purge-queue. Validate all inbound payloads using strict Zod schemas, reject unknown fields, and disallow user-supplied arbitrary URLs/methods. Route shapes should align with Runpod v2 lifecycle API while preserving a narrow server boundary.</action>
  <verify>`npm run typecheck`; API tests or manual requests confirm supported lifecycle routes succeed with valid payloads and reject malformed/extra fields with 4xx responses.</verify>
  <done>Server exposes only allowlisted lifecycle routes and cannot be used as a generic pass-through proxy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement request-scoped forwarding with key redaction and no persistence</name>
  <files>src/server/lib/runpodClient.ts, src/server/lib/redaction.ts, src/server/routes/runpodProxy.ts</files>
  <action>Implement outbound Runpod forwarding using fetch wrappers that read the Runpod API key from request-scoped input (for example request header/body field provided by authenticated client) and inject `Authorization: Bearer` only for outbound call execution. Add log redaction utilities to prevent key leakage in request/error logs. Do not write keys to disk, database, cache, or long-lived in-memory registries.</action>
  <verify>Unit/integration tests with mocked fetch confirm Authorization header is sent to Runpod, redaction hides key-like values in logs, and no persistence helper writes Runpod keys to storage.</verify>
  <done>Lifecycle requests forward correctly and API keys remain transient request-time secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Add typed client proxy helper and prove AUTH-03 with tests</name>
  <files>src/client/lib/api/runpodProxyClient.ts, src/client/features/access/RunpodProxySmoke.tsx, src/client/App.tsx, tests/server/runpodProxy.test.ts</files>
  <action>Add typed frontend helper functions to call proxy lifecycle endpoints and a minimal smoke UI section that triggers at least run + status using the current BYOK value from Plan 01-02. Add server-focused tests that cover allowlist enforcement, schema validation failures, and key-safe behavior. Keep this task focused on transport verification, not full jobs UX (deferred to Phase 4).</action>
  <verify>`npm test`; `npm run build`; manual smoke confirms run/status requests pass through proxy when invited session + API key are present.</verify>
  <done>AUTH-03 is demonstrably satisfied: lifecycle calls flow through validated proxy routes without long-term server key storage.</done>
</task>

</tasks>

<verification>
Confirm end-to-end boundary behavior:
1. Non-allowlisted proxy route attempts are rejected.
2. Valid run/status/cancel/retry/purge-queue payloads are forwarded successfully.
3. Malformed payloads fail schema validation before outbound forwarding.
4. Logs/errors never expose raw Runpod API keys.
5. No server-side persistence layer stores Runpod API keys.
</verification>

<success_criteria>
- AUTH-03 satisfied with strict server-side forwarding boundary.
- Proxy implementation is allowlisted, schema-validated, and secret-safe.
- Client can invoke lifecycle proxy calls through typed helpers using current BYOK state.
- Phase 1 success criterion for backend forwarding is met without adding a key vault.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-03-SUMMARY.md`
</output>
