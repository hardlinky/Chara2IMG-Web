---
phase: 01-access-and-proxy-boundary
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/shared/contracts/runpod.ts
  - src/server/schemas/runpodProxy.ts
  - src/server/lib/runpodClient.ts
  - src/server/lib/redaction.ts
  - src/server/routes/runpodProxy.ts
  - src/server/index.ts
autonomous: true
must_haves:
  truths:
    - Runpod lifecycle requests can be sent through backend proxy endpoints.
    - Proxy routes accept only the approved Runpod lifecycle operations needed by this product.
    - User-provided Runpod API key is forwarded per request without long-term server-side persistence.
    - Proxy behavior rejects malformed or overbroad requests instead of acting as a generic relay.
  artifacts:
    - path: src/server/routes/runpodProxy.ts
      provides: Allowlisted proxy endpoints for run/status/cancel/retry/purge-queue.
      exports: [registerRunpodProxyRoutes]
    - path: src/server/schemas/runpodProxy.ts
      provides: Strict Zod request schemas for each proxied lifecycle route.
      exports: [runRequestSchema, statusRequestSchema, cancelRequestSchema, retryRequestSchema, purgeQueueRequestSchema]
    - path: src/server/lib/runpodClient.ts
      provides: Runpod fetch wrapper that injects Authorization header from request-scoped key.
      exports: [forwardRunpodRequest]
  key_links:
    - from: src/server/routes/runpodProxy.ts
      to: src/server/lib/runpodClient.ts
      via: Per-route validated payload forwarded to Runpod API
      pattern: forwardRunpodRequest
    - from: src/server/lib/runpodClient.ts
      to: https://api.runpod.ai/v2/{endpointId}
      via: Outbound lifecycle fetch with request-scoped bearer header
      pattern: Authorization: Bearer
---

<objective>
Implement the thin Runpod proxy core with strict validation and secret-safe forwarding.

Purpose: Deliver AUTH-03 backend boundary without coupling to frontend smoke proving work in the same plan.
Output: Validated allowlist proxy routes, strict request schemas, and request-scoped forwarding utilities.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-RESEARCH.md
@.gsd/phases/01-access-and-proxy-boundary/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build strict allowlist Runpod proxy contract and server routes</name>
  <files>src/shared/contracts/runpod.ts, src/server/schemas/runpodProxy.ts, src/server/routes/runpodProxy.ts, src/server/index.ts</files>
  <action>Create explicit proxy endpoints for run, status, cancel, retry, and purge-queue. Validate all inbound payloads with strict Zod schemas, reject unknown fields, and disallow user-supplied arbitrary URLs/methods. Keep route surface narrow to the app lifecycle needs.</action>
  <verify>`npm run typecheck`; API checks confirm supported routes succeed with valid payloads and malformed/extra fields are rejected with 4xx responses.</verify>
  <done>Server exposes only allowlisted lifecycle routes and cannot be used as a generic pass-through proxy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement request-scoped forwarding and key redaction behavior</name>
  <files>src/server/lib/runpodClient.ts, src/server/lib/redaction.ts, src/server/routes/runpodProxy.ts</files>
  <action>Implement outbound forwarding that reads Runpod API key from request-scoped input and injects `Authorization: Bearer` only for outbound execution. Add redaction utilities to prevent key leakage in logs/errors. Do not write keys to disk, database, cache, or long-lived in-memory registries.</action>
  <verify>Unit/integration checks with mocked fetch confirm Authorization header forwarding, redaction of key-like values, and no persistence helper writes Runpod keys to storage.</verify>
  <done>AUTH-03 proxy core forwards lifecycle requests correctly while keeping API keys transient.</done>
</task>

</tasks>

<verification>
Confirm backend forwarding boundary behavior:
1. Non-allowlisted proxy route attempts are rejected.
2. Valid run/status/cancel/retry/purge-queue payloads are forwarded.
3. Malformed payloads fail schema validation before outbound calls.
4. Logs/errors never expose raw Runpod API keys.
5. No server-side persistence layer stores Runpod API keys.
</verification>

<success_criteria>
- AUTH-03 backend proxy boundary is implemented and allowlisted.
- Request validation and key-safe forwarding are enforced on server routes.
- No long-term server-side key persistence is introduced.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-04-SUMMARY.md`
</output>