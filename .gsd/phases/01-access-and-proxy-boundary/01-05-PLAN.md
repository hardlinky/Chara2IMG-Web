---
phase: 01-access-and-proxy-boundary
plan: 05
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - src/client/lib/api/runpodProxyClient.ts
  - src/client/features/access/RunpodProxySmoke.tsx
  - src/client/App.tsx
  - tests/server/runpodProxy.test.ts
autonomous: true
must_haves:
  truths:
    - Client can call validated Runpod proxy lifecycle routes through typed helpers.
    - Smoke flow proves run/status proxy path works when invited session and BYOK are present.
    - Tests prove allowlist and key-safe behavior for the proxy boundary.
  artifacts:
    - path: src/client/lib/api/runpodProxyClient.ts
      provides: Typed client helpers for calling `/api/runpod/*` endpoints.
    - path: src/client/features/access/RunpodProxySmoke.tsx
      provides: Minimal UI flow to trigger and observe run/status forwarding behavior.
    - path: tests/server/runpodProxy.test.ts
      provides: Coverage for allowlist validation, schema failures, and key-safe behavior.
  key_links:
    - from: src/client/lib/api/runpodProxyClient.ts
      to: /api/runpod/*
      via: Typed client lifecycle helper calls
      pattern: /api/runpod
    - from: tests/server/runpodProxy.test.ts
      to: src/server/routes/runpodProxy.ts
      via: Route contract tests verify allowlist and validation behavior
      pattern: runpodProxy
---

<objective>
Prove the Runpod proxy boundary through typed client wiring, smoke UX, and automated tests.

Purpose: Separate proof/integration work from proxy core implementation so plan scope stays narrow and test-focused.
Output: Typed client proxy helper, minimal smoke UI, and server tests that validate AUTH-03 behavior.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/01-access-and-proxy-boundary/01-03-SUMMARY.md
@.gsd/phases/01-access-and-proxy-boundary/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed client proxy helpers and smoke surface</name>
  <files>src/client/lib/api/runpodProxyClient.ts, src/client/features/access/RunpodProxySmoke.tsx, src/client/App.tsx</files>
  <action>Add typed frontend helper functions for run and status proxy calls, then wire a minimal smoke section that uses the current BYOK value from Plan 01-03. Keep this as transport verification only; full jobs UX remains deferred to Phase 4.</action>
  <verify>`npm run build`; manual smoke confirms run/status requests pass through proxy when invited session and API key are present.</verify>
  <done>Client can exercise proxy lifecycle routes through typed helpers using current BYOK state.</done>
</task>

<task type="auto">
  <name>Task 2: Add proxy boundary tests covering allowlist and key safety</name>
  <files>tests/server/runpodProxy.test.ts</files>
  <action>Add server-focused tests for allowlist enforcement, schema validation failure paths, and key-safe behavior (no secret leaks/no persistence assumptions). Ensure tests assert rejection of unsupported routes and malformed payloads.</action>
  <verify>`npm test`; tests pass and cover allowlist + validation + key-safe expectations.</verify>
  <done>AUTH-03 is demonstrably proven with repeatable smoke and test evidence.</done>
</task>

</tasks>

<verification>
Confirm integration proof:
1. Run/status proxy calls succeed through typed client helpers.
2. Invalid/malformed requests fail with expected validation errors.
3. Test suite verifies allowlist boundaries and secret-safe handling.
</verification>

<success_criteria>
- AUTH-03 is proven by smoke flow and automated tests, not only by implementation intent.
- Client proxy usage is typed and aligned with allowlisted backend contracts.
- Proxy safety guarantees remain intact under test.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-access-and-proxy-boundary/01-05-SUMMARY.md`
</output>