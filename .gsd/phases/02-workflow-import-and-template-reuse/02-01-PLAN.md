---
phase: 02-workflow-import-and-template-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/workflow/importWorkflow.ts
  - src/shared/workflow/workflowSchemas.ts
  - src/shared/contracts/workflow.ts
  - src/client/App.tsx
  - src/client/features/workflows/WorkflowImport.tsx
  - tests/client/workflowImport.test.ts
  - tests/client/fixtures/workflows/README.md
  - tests/client/fixtures/workflows/comfyui-valid-template.json
  - tests/client/fixtures/workflows/comfyui-invalid-template-missing-input-node.json
  - tests/client/fixtures/workflows/wpf-legacy-template.json
autonomous: true
must_haves:
  truths:
    - "User can upload a ComfyUI workflow JSON file and see separate shape-validation and template-rule validation results before any job run is attempted."
    - "The imported workflow is preserved as the original raw JSON source of truth, not rewritten into a reduced input-only form."
    - "The repo has a committed workflow validation fixture corpus that covers at least one real valid export and one malformed or near-miss case."
  artifacts:
    - path: "src/shared/workflow/importWorkflow.ts"
      provides: "Two-stage workflow validation and normalization entry points"
    - path: "src/shared/workflow/workflowSchemas.ts"
      provides: "Zod-backed ComfyUI shape checks and Chara2Img template rules"
    - path: "src/client/features/workflows/WorkflowImport.tsx"
      provides: "Workflow upload and validation UX"
    - path: "tests/client/workflowImport.test.ts"
      provides: "Fixture-driven validation coverage"
    - path: "tests/client/fixtures/workflows/comfyui-valid-template.json"
      provides: "Known-good ComfyUI import sample"
    - path: "tests/client/fixtures/workflows/comfyui-invalid-template-missing-input-node.json"
      provides: "Template-rule failure sample"
    - path: "tests/client/fixtures/workflows/wpf-legacy-template.json"
      provides: "Legacy export sample for regression coverage"
  key_links:
    - from: "src/client/features/workflows/WorkflowImport.tsx"
      to: "src/shared/workflow/importWorkflow.ts"
      via: "file input handler and validation display"
      pattern: "validate.*workflow|normalize.*workflow"
    - from: "src/shared/workflow/importWorkflow.ts"
      to: "src/shared/contracts/workflow.ts"
      via: "shared import result and template record types"
      pattern: "export type|interface"
    - from: "tests/client/workflowImport.test.ts"
      to: "tests/client/fixtures/workflows/"
      via: "fixture-driven import assertions"
      pattern: "fixtures|readFile"

<objective>
Build the workflow import boundary that accepts uploaded ComfyUI JSON, validates it in two stages, and preserves the raw payload unchanged for later reuse.

Purpose: Phase 2 must solve fidelity first so later editing and rerun work can trust the imported template.
Output: Shared import/validation helpers, a client upload entry point, and a real fixture corpus with regression tests.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/02-workflow-import-and-template-reuse/02-RESEARCH.md
@package.json
@src/client/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Create shared workflow import and validation entry points</name>
  <files>src/shared/workflow/importWorkflow.ts, src/shared/workflow/workflowSchemas.ts, src/shared/contracts/workflow.ts</files>
  <action>Define the raw canonical workflow record plus derived metadata types, then implement the two-stage pipeline the research called for: first Zod-backed ComfyUI shape validation, then Chara2Img-specific template rules. The normalization helper must keep the raw imported JSON unchanged as the source of truth while deriving only stable metadata such as fingerprint, display name, and validation state for downstream use.</action>
  <verify>Run `npm test -- tests/client/workflowImport.test.ts` after the test file exists and confirm shape failures and template-rule failures are reported separately.</verify>
  <done>Import callers can distinguish malformed workflow JSON from valid-but-not-reusable templates, and the normalized record still contains the untouched raw JSON payload.</done>
</task>

<task type="auto">
  <name>Wire the workflow import UI into the client shell</name>
  <files>src/client/App.tsx, src/client/features/workflows/WorkflowImport.tsx</files>
  <action>Add the client entry point that accepts a `.json` file, reads it as text, passes it through the shared import pipeline, and shows the shape-valid/template-valid state separately. Keep the uploaded workflow in local component state as the raw template source so the UI does not collapse it into an extracted-input representation.</action>
  <verify>Run `npm run build` and confirm the client compiles with the new workflow import surface mounted in the app shell.</verify>
  <done>The app exposes a working workflow import panel that can report validity without mutating the imported template.</done>
</task>

<task type="auto">
  <name>Seed the workflow fixture corpus and regression coverage</name>
  <files>tests/client/workflowImport.test.ts, tests/client/fixtures/workflows/README.md, tests/client/fixtures/workflows/comfyui-valid-template.json, tests/client/fixtures/workflows/comfyui-invalid-template-missing-input-node.json, tests/client/fixtures/workflows/wpf-legacy-template.json</files>
  <action>Create a committed validation corpus from real ComfyUI and WPF exports, plus at least one malformed or near-miss example, and document the corpus source requirements in the fixture README. If a source export is not available in the repo yet, isolate that gap in the README instead of silently downgrading the coverage to synthetic-only samples. Drive the import tests from those fixtures so the phase does not depend on one happy-path workflow.</action>
  <verify>Run `npm test -- tests/client/workflowImport.test.ts` and confirm the suite exercises the committed fixtures instead of inline ad hoc JSON strings.</verify>
  <done>The repository contains a reusable workflow fixture corpus and tests that lock in the fidelity-preserving import rules.</done>
</task>

</tasks>

<verification>
`npm test -- tests/client/workflowImport.test.ts`
`npm run build`
</verification>

<success_criteria>
Workflow JSON can be uploaded, validated in two stages, and preserved verbatim as the template source, with corpus-backed regression coverage proving the behavior.
</success_criteria>

<output>
After completion, create `.gsd/phases/02-workflow-import-and-template-reuse/02-01-SUMMARY.md`
</output>
