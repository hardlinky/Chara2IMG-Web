---
phase: 02-workflow-import-and-template-reuse
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - package-lock.json
  - src/client/lib/workflowStorage.ts
  - src/client/features/workflows/ActiveWorkflowTemplate.tsx
  - src/client/features/workflows/useActiveWorkflowTemplate.ts
  - src/client/App.tsx
  - tests/client/workflowStorage.test.ts
  - tests/client/workflowReuse.test.ts
  - tests/client/fixtures/workflows/README.md
autonomous: true
must_haves:
  truths:
    - "A validated workflow template can be stored locally and recovered after a browser refresh without re-uploading the file."
    - "The saved template remains the full original JSON payload, not a reduced copy of extracted inputs."
    - "The app maintains one active template slot: importing a new parseable template replaces the previous active template for later runs."
  artifacts:
    - path: "src/client/lib/workflowStorage.ts"
      provides: "Dexie-backed IndexedDB template persistence"
    - path: "src/client/features/workflows/ActiveWorkflowTemplate.tsx"
      provides: "Single active-template restore and replacement UI"
    - path: "src/client/features/workflows/useActiveWorkflowTemplate.ts"
      provides: "Client-side single-slot template loading and refresh restoration"
    - path: "tests/client/workflowStorage.test.ts"
      provides: "Storage round-trip coverage"
    - path: "tests/client/workflowReuse.test.ts"
      provides: "Reuse-across-runs coverage"
  key_links:
    - from: "src/client/lib/workflowStorage.ts"
      to: "IndexedDB"
      via: "Dexie versioned table"
      pattern: 'new Dexie|version\('
    - from: "src/client/features/workflows/ActiveWorkflowTemplate.tsx"
      to: "src/client/lib/workflowStorage.ts"
      via: "load and replace the single active template"
      pattern: "get|put|delete"
    - from: "src/client/App.tsx"
      to: "src/client/features/workflows/useActiveWorkflowTemplate.ts"
      via: "active template state for repeated runs"
      pattern: "useActiveWorkflowTemplate|activeTemplate"

<objective>
Persist one reusable active workflow template locally in IndexedDB so users can return to a validated template across sessions and keep reusing it without re-uploading.

Purpose: Phase 2 must end with browser-local, full-fidelity template reuse and no server-side workflow persistence.
Output: A versioned Dexie store for a single active template, an active-template restore surface, and round-trip tests proving replacement and reuse behavior.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/02-workflow-import-and-template-reuse/02-RESEARCH.md
@src/client/App.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Add the Dexie-backed workflow template store</name>
  <files>package.json, package-lock.json, src/client/lib/workflowStorage.ts</files>
  <action>Install Dexie and implement a versioned IndexedDB template store that saves the raw workflow JSON verbatim plus derived metadata such as fingerprint, display name, schemaVersion, importedAt, and validation state. Implement a single active-template slot keyed for replacement semantics (new import overwrites prior active template). Keep the store browser-local only, and do not add localStorage or any server persistence for templates. Reuse the shared workflow contract types created in plan 02-01 instead of redefining them here.</action>
  <verify>Run `npm run build` and the storage test suite to confirm the store compiles and the template record shape is stable.</verify>
  <done>Validated parseable workflow templates can be written to and read back from the single active IndexedDB slot without losing fidelity or depending on server storage.</done>
</task>

<task type="auto">
  <name>Build the active-template restore and replace flow</name>
  <files>src/client/App.tsx, src/client/features/workflows/ActiveWorkflowTemplate.tsx, src/client/features/workflows/useActiveWorkflowTemplate.ts</files>
  <action>Surface the persisted active workflow template in the client shell, load it on startup, and keep the selected raw workflow available for repeated runs without another upload. When a new parseable template is imported, replace the previously active stored template and update UI state accordingly. Reuse the canonical stored record directly rather than rebuilding a reduced input model from scratch.</action>
  <verify>Run `npm run build` and confirm the app can mount with a selected saved template available for reuse after refresh.</verify>
  <done>A previously imported workflow can be restored after refresh, and importing a new template replaces the previous active template for subsequent runs.</done>
</task>

<task type="auto">
  <name>Add round-trip and reuse regression tests</name>
  <files>tests/client/workflowStorage.test.ts, tests/client/workflowReuse.test.ts, tests/client/fixtures/workflows/README.md</files>
  <action>Write tests that save a validated template, reload it, and reuse it across two simulated job runs while preserving the original raw JSON and a stable fingerprint. Add replacement tests that import a second parseable template and confirm it overwrites the first active template slot. Document the fixture corpus source requirements in the README so the browser-local active-template flow survives a refresh boundary without mutating the source workflow.</action>
  <verify>Run `npm test -- tests/client/workflowStorage.test.ts tests/client/workflowReuse.test.ts` and confirm the same stored template can be selected more than once.</verify>
  <done>Template round-tripping, active-template replacement-on-import, and multi-run reuse are covered by automated regression tests.</done>
</task>

</tasks>

<verification>
`npm test -- tests/client/workflowStorage.test.ts tests/client/workflowReuse.test.ts`
`npm run build`
</verification>

<success_criteria>
The app can persist one validated active workflow template locally, restore it after refresh, replace it when a new template is imported, and reuse the active raw template across multiple runs without re-uploading or rewriting it.
</success_criteria>

<output>
After completion, create `.gsd/phases/02-workflow-import-and-template-reuse/02-02-SUMMARY.md`
</output>
