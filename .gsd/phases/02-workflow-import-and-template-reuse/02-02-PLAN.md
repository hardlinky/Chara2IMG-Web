---
phase: 02-workflow-import-and-template-reuse
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - package-lock.json
  - src/client/lib/workflowStorage.ts
  - src/client/features/workflows/WorkflowTemplatePicker.tsx
  - src/client/features/workflows/useWorkflowTemplates.ts
  - src/client/App.tsx
  - src/shared/contracts/workflow.ts
  - tests/client/workflowStorage.test.ts
  - tests/client/workflowReuse.test.ts
autonomous: true
must_haves:
  truths:
    - "A validated workflow template can be stored locally and recovered after a browser refresh without re-uploading the file."
    - "The saved template remains the full original JSON payload, not a reduced copy of extracted inputs."
    - "The same stored template can be selected and reused across multiple job runs."
  artifacts:
    - path: "src/client/lib/workflowStorage.ts"
      provides: "Dexie-backed IndexedDB template persistence"
    - path: "src/client/features/workflows/WorkflowTemplatePicker.tsx"
      provides: "Saved-template selection and restore UI"
    - path: "src/client/features/workflows/useWorkflowTemplates.ts"
      provides: "Client-side template loading and refresh restoration"
    - path: "tests/client/workflowStorage.test.ts"
      provides: "Storage round-trip coverage"
    - path: "tests/client/workflowReuse.test.ts"
      provides: "Reuse-across-runs coverage"
  key_links:
    - from: "src/client/lib/workflowStorage.ts"
      to: "IndexedDB"
      via: "Dexie versioned table"
      pattern: 'new Dexie|version\('
    - from: "src/client/features/workflows/WorkflowTemplatePicker.tsx"
      to: "src/client/lib/workflowStorage.ts"
      via: "load saved templates on startup and on selection changes"
      pattern: "toArray|get|put"
    - from: "src/client/App.tsx"
      to: "src/client/features/workflows/useWorkflowTemplates.ts"
      via: "selected template state for repeated runs"
      pattern: "useWorkflowTemplates|selectedTemplate"

<objective>
Persist reusable workflow templates locally in IndexedDB so users can return to a validated template across sessions and keep reusing it without re-uploading.

Purpose: Phase 2 must end with browser-local, full-fidelity template reuse and no server-side workflow persistence.
Output: A versioned Dexie store, a template picker/restore surface, and round-trip tests proving reuse behavior.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/02-workflow-import-and-template-reuse/02-RESEARCH.md
@src/client/App.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Add the Dexie-backed workflow template store</name>
  <files>package.json, package-lock.json, src/client/lib/workflowStorage.ts, src/shared/contracts/workflow.ts</files>
  <action>Install Dexie and implement a versioned IndexedDB template store that saves the raw workflow JSON verbatim plus derived metadata such as fingerprint, display name, schemaVersion, importedAt, and validation state. Keep the store browser-local only, and do not add localStorage or any server persistence for templates.</action>
  <verify>Run `npm run build` and the storage test suite to confirm the store compiles and the template record shape is stable.</verify>
  <done>Validated workflow templates can be written to and read back from IndexedDB without losing fidelity or depending on server storage.</done>
</task>

<task type="auto">
  <name>Build the saved-template picker and restore flow</name>
  <files>src/client/App.tsx, src/client/features/workflows/WorkflowTemplatePicker.tsx, src/client/features/workflows/useWorkflowTemplates.ts</files>
  <action>Surface persisted workflow templates in the client shell, load them on startup, let the user pick a saved template, and keep the selected raw workflow available for repeated runs without another upload. Reuse the canonical stored record directly rather than rebuilding a reduced input model from scratch.</action>
  <verify>Run `npm run build` and confirm the app can mount with a selected saved template available for reuse after refresh.</verify>
  <done>A previously imported workflow can be reopened and selected again without re-uploading the file.</done>
</task>

<task type="auto">
  <name>Add round-trip and reuse regression tests</name>
  <files>tests/client/workflowStorage.test.ts, tests/client/workflowReuse.test.ts</files>
  <action>Write tests that save a validated template, reload it, and reuse it across two simulated job runs while preserving the original raw JSON and a stable fingerprint. The tests should prove that the browser-local template store and picker flow survive a refresh boundary without mutating the source workflow.</action>
  <verify>Run `npm test -- tests/client/workflowStorage.test.ts tests/client/workflowReuse.test.ts` and confirm the same stored template can be selected more than once.</verify>
  <done>Template round-tripping and multi-run reuse are covered by automated regression tests.</done>
</task>

</tasks>

<verification>
`npm test -- tests/client/workflowStorage.test.ts tests/client/workflowReuse.test.ts`
`npm run build`
</verification>

<success_criteria>
The app can persist a validated workflow template locally, restore it after refresh, and reuse the same raw template across multiple runs without re-uploading or rewriting it.
</success_criteria>

<output>
After completion, create `.gsd/phases/02-workflow-import-and-template-reuse/02-02-SUMMARY.md`
</output>
