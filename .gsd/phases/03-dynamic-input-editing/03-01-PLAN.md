---
phase: 03-dynamic-input-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/inputs.ts
  - src/shared/workflow/deriveInputControls.ts
  - src/client/lib/inputEditorStorage.ts
  - src/client/features/inputs/useDynamicInputEditor.ts
  - src/client/features/inputs/DynamicInputEditor.tsx
  - src/client/App.tsx
  - tests/shared/deriveInputControls.test.ts
  - tests/client/dynamicInputEditor.test.ts
autonomous: true
must_haves:
  truths:
    - "User can see generated input controls from supported workflow nodes in stable category grouping and ordering derived from [Input#] title conventions."
    - "User can edit text, multiline text, numeric, boolean, dimension, and image controls from the same loaded template without exposing workflow internals."
    - "Input ordering remains repeatable across sessions by applying default derived ordering first, then a global user reorder overlay."
  artifacts:
    - path: "src/shared/workflow/deriveInputControls.ts"
      provides: "Deterministic [Input#] parsing, category/name extraction, control typing, warnings, and default ordering"
    - path: "src/shared/contracts/inputs.ts"
      provides: "Shared control, warning, ordering, and draft contracts used by parser and UI"
    - path: "src/client/features/inputs/DynamicInputEditor.tsx"
      provides: "Workflow-derived control renderer for supported input types"
    - path: "src/client/features/inputs/useDynamicInputEditor.ts"
      provides: "Client state orchestration for draft edits, warnings, and ordering overlay"
    - path: "src/client/lib/inputEditorStorage.ts"
      provides: "Browser-local draft and ordering overlay persistence"
    - path: "tests/shared/deriveInputControls.test.ts"
      provides: "Fixture-backed parser/order regression coverage"
  key_links:
    - from: "src/client/features/inputs/useDynamicInputEditor.ts"
      to: "src/shared/workflow/deriveInputControls.ts"
      via: "derive control catalog from active template rawJson"
      pattern: "deriveInputControls"
    - from: "src/client/features/inputs/DynamicInputEditor.tsx"
      to: "src/client/features/inputs/useDynamicInputEditor.ts"
      via: "render ordered sections and bind edit handlers"
      pattern: "useDynamicInputEditor|onChange"
    - from: "src/client/lib/inputEditorStorage.ts"
      to: "src/client/features/inputs/useDynamicInputEditor.ts"
      via: "restore and persist drafts and ordering overlay"
      pattern: "save|load|overlay"
---

<objective>
Generate a deterministic, workflow-derived input editor surface with typed controls, stable grouping/order behavior, and browser-local draft persistence.

Purpose: Phase 3 starts by making the input surface trustworthy and repeatable before run-time apply-back logic is introduced.
Output: Shared derivation contracts/helpers, input editor feature modules, and fixture-backed parser/editor tests proving INPT-01 through INPT-06 and INPT-08 foundations.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03-dynamic-input-editing/03-CONTEXT.md
@.gsd/phases/03-dynamic-input-editing/03-RESEARCH.md
@.gsd/phases/02-workflow-import-and-template-reuse/02-01-SUMMARY.md
@.gsd/phases/02-workflow-import-and-template-reuse/02-02-SUMMARY.md
@src/shared/contracts/workflow.ts
@src/shared/workflow/importWorkflow.ts
@src/client/features/workflows/useActiveWorkflowTemplate.ts
@src/client/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Create shared dynamic-input contracts and derivation pipeline</name>
  <files>src/shared/contracts/inputs.ts, src/shared/workflow/deriveInputControls.ts, tests/shared/deriveInputControls.test.ts</files>
  <action>Define explicit shared types for control kinds, value shapes, section/group ordering metadata, source mapping metadata, and non-blocking warnings. Implement deriveInputControls from workflow rawJson using locked rules: case-sensitive exact [Input#] prefix, first-dot split for Category.Name, trim surrounding whitespace, allow duplicates and numbering gaps, exclude disallowed symbols with warnings, fallback unclean titles to Uncategorized, exclude unsupported/missing editable-value nodes with warnings, and default sort by parsed input index then full title tie-break. Use `_meta.title` as default title source with `inputs.title` fallback, and include first-class Primitive node defaults (`inputs.value`) for text/multiline/boolean/number controls. Add dedicated `Detailer.Loras` row mapping for `lora_*` entries (toggle, lora label, strength slider/textbox). Add fixture-driven tests that cover duplicates, numbering gaps, invalid symbols, multiline name segments, unsupported types, missing editable fields, lora-row mapping, and deterministic ordering.</action>
  <verify>Run `npm test -- tests/shared/deriveInputControls.test.ts` and confirm deterministic control counts, warning emission, and ordering behavior from fixture workflows.</verify>
  <done>Shared derivation produces stable typed controls + warnings from imported workflows with locked parser/order behavior proven by automated tests.</done>
</task>

<task type="auto">
  <name>Implement browser-local draft and ordering overlay state orchestration</name>
  <files>src/client/lib/inputEditorStorage.ts, src/client/features/inputs/useDynamicInputEditor.ts</files>
  <action>Create persistence helpers for per-template draft values and global ordering overlays using browser-local storage patterns already used in the app. In the feature hook, compute default order from deriveInputControls first, then apply saved overlay, ignoring stale overlay entries silently and preserving insertion of newly discovered controls by default order. Enforce draft reset semantics from locked decisions: template load starts from template defaults, auto-save edits continuously, and reset action availability is based on diff from template defaults. Keep source mapping hidden by default with a user-controlled toggle state.</action>
  <verify>Run `npm test -- tests/client/dynamicInputEditor.test.ts` after Task 3 and confirm draft restore/reset and overlay merge behavior for new/stale controls.</verify>
  <done>Input drafts and ordering overlays persist locally without mutating template defaults, and ordering remains stable when templates evolve.</done>
</task>

<task type="auto">
  <name>Build and mount the dynamic input editor surface</name>
  <files>src/client/features/inputs/DynamicInputEditor.tsx, src/client/App.tsx, tests/client/dynamicInputEditor.test.ts</files>
  <action>Build a DynamicInputEditor component that renders sections and controls for supported types (text, multiline text, numeric, boolean, dimension pair, image picker with preview and replace/remove) from the hook state. Show plain-language non-blocking warnings for excluded/unsupported nodes without exposing workflow internals. Add unsaved-change and inline-invalid visual state support but keep editing/navigation non-blocking. Mount the editor in App only when an active template exists, preserving the workflow-as-black-box UI boundary. Add client tests for grouped rendering, control editing interactions, warning display, and reset-visibility toggling.</action>
  <verify>Run `npm test -- tests/client/dynamicInputEditor.test.ts` and `npm run build`.</verify>
  <done>Users can load an active template and interact with a stable, typed, workflow-derived input editor that supports all required control classes with non-blocking inline feedback.</done>
</task>

</tasks>

<verification>
`npm test -- tests/shared/deriveInputControls.test.ts tests/client/dynamicInputEditor.test.ts`
`npm run build`
</verification>

<success_criteria>
Workflow-derived controls are generated deterministically with locked parsing/order rules, users can edit all supported input types with non-blocking inline feedback, and browser-local drafts plus ordering overlays keep the input tab repeatable across sessions.
</success_criteria>

<output>
After completion, create `.gsd/phases/03-dynamic-input-editing/03-01-SUMMARY.md`
</output>
