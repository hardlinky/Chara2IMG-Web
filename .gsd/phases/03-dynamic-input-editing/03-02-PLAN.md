---
phase: 03-dynamic-input-editing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/shared/contracts/inputs.ts
  - src/shared/workflow/validateInputDraft.ts
  - src/shared/workflow/buildRunWorkflowPayload.ts
  - src/client/features/inputs/useDynamicInputEditor.ts
  - src/client/features/inputs/DynamicInputEditor.tsx
  - src/client/App.tsx
  - tests/shared/validateInputDraft.test.ts
  - tests/shared/buildRunWorkflowPayload.test.ts
  - tests/client/dynamicInputRunValidation.test.ts
autonomous: true
must_haves:
  truths:
    - "On Run, the app blocks submission when any required input is invalid, highlights invalid fields, and preserves all user edits for correction."
    - "When all fields are valid, the app builds a fresh job-ready workflow payload from canonical template plus current draft values and never mutates the canonical template."
    - "Input write-back is all-or-nothing: any write failure prevents the run request and reports plain-language field-level errors."
  artifacts:
    - path: "src/shared/workflow/validateInputDraft.ts"
      provides: "Hybrid validation helpers for inline checks and blocking run-time validation"
    - path: "src/shared/workflow/buildRunWorkflowPayload.ts"
      provides: "Fresh payload construction and deterministic write-back from draft values"
    - path: "src/client/features/inputs/useDynamicInputEditor.ts"
      provides: "Run attempt orchestration that combines validation, payload build, and error state management"
    - path: "src/client/features/inputs/DynamicInputEditor.tsx"
      provides: "Run-triggered invalid highlighting and blocking messaging"
    - path: "tests/shared/buildRunWorkflowPayload.test.ts"
      provides: "Apply-back fidelity and canonical-template immutability tests"
    - path: "tests/client/dynamicInputRunValidation.test.ts"
      provides: "Run-blocking UX and state preservation tests"
  key_links:
    - from: "src/client/features/inputs/useDynamicInputEditor.ts"
      to: "src/shared/workflow/validateInputDraft.ts"
      via: "inline and run-time validation execution"
      pattern: "validate.*inline|validate.*run"
    - from: "src/client/features/inputs/useDynamicInputEditor.ts"
      to: "src/shared/workflow/buildRunWorkflowPayload.ts"
      via: "run click builds payload from canonical template + current draft"
      pattern: "buildRunWorkflowPayload"
    - from: "src/shared/workflow/buildRunWorkflowPayload.ts"
      to: "src/shared/contracts/workflow.ts"
      via: "structured clone of template rawJson to preserve canonical source"
      pattern: "structuredClone|rawJson"
---

<objective>
Apply edited dynamic-input values back into a validated, job-ready workflow payload at Run time with all-or-nothing integrity and canonical-template immutability.

Purpose: Phase 3 must complete the correctness path so edited controls become trustworthy run payloads without leaking workflow internals or corrupting template state.
Output: Shared validation and apply-back modules, run-time editor wiring, and automated tests proving INPT-07 plus run-blocking behavior expectations.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03-dynamic-input-editing/03-CONTEXT.md
@.gsd/phases/03-dynamic-input-editing/03-RESEARCH.md
@.gsd/phases/03-dynamic-input-editing/03-01-PLAN.md
@src/shared/contracts/workflow.ts
@src/client/lib/api/runpodProxyClient.ts
@src/client/features/inputs/useDynamicInputEditor.ts
@src/client/features/inputs/DynamicInputEditor.tsx
@src/client/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Add hybrid validation primitives for dynamic input drafts</name>
  <files>src/shared/contracts/inputs.ts, src/shared/workflow/validateInputDraft.ts, tests/shared/validateInputDraft.test.ts</files>
  <action>Extend shared input contracts with explicit validation result shapes for inline field checks and blocking run-time validation summaries. Implement validation helpers that keep inline checks non-blocking during edit events, remove inline errors immediately when locally valid, and produce full blocking validation output on Run with plain-language messages only. Enforce control-type-specific rules from metadata constraints (no heuristic coercion), including required image presence, numeric bounds/precision rules, and dimension-pair completeness. Keep structured invalid drafts from being persisted while allowing text-like invalid drafts per locked decisions.</action>
  <verify>Run `npm test -- tests/shared/validateInputDraft.test.ts` and confirm inline-vs-run behavior, image-required blocking, and immediate inline-error clearing cases.</verify>
  <done>Validation behavior is codified in shared helpers and proven with tests for non-blocking edit checks and blocking run checks across all supported control types.</done>
</task>

<task type="auto">
  <name>Implement canonical-template apply-back payload builder</name>
  <files>src/shared/workflow/buildRunWorkflowPayload.ts, tests/shared/buildRunWorkflowPayload.test.ts</files>
  <action>Create buildRunWorkflowPayload that takes canonical template rawJson plus validated draft values, performs a deep clone each run, applies mapped values to target nodes/fields, and returns a run-ready payload object. Enforce all-or-nothing semantics: if any required write-back target is missing or incompatible, return blocking errors and no payload. Ensure canonical template remains unchanged, preserve unmapped workflow sections, and support cross-workflow overlay semantics by leaving unmatched fields at template defaults. Add tests for successful write-back, missing-target failure, immutability of source template, and deterministic payload content.</action>
  <verify>Run `npm test -- tests/shared/buildRunWorkflowPayload.test.ts` and confirm successful and failure-path assertions, including immutability checks.</verify>
  <done>Run payload construction is deterministic, immutable against template source, and blocks on any write-back failure instead of producing partial payloads.</done>
</task>

<task type="auto">
  <name>Wire Run-time validation and payload build into editor flow</name>
  <files>src/client/features/inputs/useDynamicInputEditor.ts, src/client/features/inputs/DynamicInputEditor.tsx, src/client/App.tsx, tests/client/dynamicInputRunValidation.test.ts</files>
  <action>Integrate the run action path so clicking Run triggers blocking validation, highlights invalid controls, preserves user edits, and skips run dispatch when invalid. On valid run, build fresh payload from canonical template and clear run-validation error state after success. Keep unsaved-changes semantics tied to diff from last successful run snapshot and preserve no-separate-Apply behavior. Add tests covering blocked run on invalid fields, retained edits after failed run, successful payload build trigger on valid input, and validation-state reset after successful run.</action>
  <verify>Run `npm test -- tests/client/dynamicInputRunValidation.test.ts` and `npm run build`.</verify>
  <done>Run attempts now enforce full validation and all-or-nothing apply-back, with clear invalid highlighting and preserved draft state until users fix only failing fields.</done>
</task>

</tasks>

<verification>
`npm test -- tests/shared/validateInputDraft.test.ts tests/shared/buildRunWorkflowPayload.test.ts tests/client/dynamicInputRunValidation.test.ts`
`npm run build`
</verification>

<success_criteria>
Before any Run request, edited inputs are fully validated and either block with actionable field highlights while preserving user edits, or produce a fresh canonical-safe job-ready payload with deterministic apply-back behavior.
</success_criteria>

<output>
After completion, create `.gsd/phases/03-dynamic-input-editing/03-02-SUMMARY.md`
</output>
