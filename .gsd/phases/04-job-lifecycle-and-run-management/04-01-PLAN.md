---
phase: 04-job-lifecycle-and-run-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/jobs.ts
  - src/client/lib/recentJobsStorage.ts
  - src/client/lib/api/runpodProxyClient.ts
  - src/client/App.tsx
  - tests/shared/jobContracts.test.ts
  - tests/client/recentJobsStorage.test.ts
  - tests/client/appJobSubmission.test.tsx
autonomous: true
must_haves:
  truths:
    - "Submitting a run creates a recent-job record that keeps endpoint, template fingerprint, and input provenance needed for later lifecycle actions."
    - "A submitted job appears in the app-local recent jobs domain immediately after a successful Runpod run response."
    - "Recent job records persist across reloads and are pruned by a deterministic retention policy so normal usage does not grow unbounded."
  artifacts:
    - path: "src/shared/contracts/jobs.ts"
      provides: "Typed recent-job record, status snapshot, terminal reason, and retention constants used by client lifecycle flows"
    - path: "src/client/lib/recentJobsStorage.ts"
      provides: "Dexie-backed recent jobs store with insert, read, and prune helpers"
    - path: "src/client/App.tsx"
      provides: "Submission wiring that saves submitted jobs with workflow-input provenance"
    - path: "src/client/lib/api/runpodProxyClient.ts"
      provides: "Typed run/status response shapes used to derive initial recent-job status"
    - path: "tests/client/recentJobsStorage.test.ts"
      provides: "Retention and persistence regression coverage for recent-job storage"
  key_links:
    - from: "src/client/App.tsx"
      to: "src/client/lib/api/runpodProxyClient.ts"
      via: "run submit path uses typed run response to extract job id and status"
      pattern: "runViaProxy"
    - from: "src/client/App.tsx"
      to: "src/client/lib/recentJobsStorage.ts"
      via: "successful submission writes recent-job record"
      pattern: "saveRecentJob|upsertRecentJob"
    - from: "src/client/lib/recentJobsStorage.ts"
      to: "src/shared/contracts/jobs.ts"
      via: "store row shape and retention policy constants"
      pattern: "RecentJobRecord|RECENT_JOBS_"
---

<objective>
Establish a typed recent-jobs domain and wire job submission into persistent lifecycle records.

Purpose: Phase 4 starts by guaranteeing every successful submission has durable provenance before polling and job controls are layered on top.
Output: Shared jobs contracts, persistent recent-jobs storage, and submission wiring that records run results for JOBS-01 and JOBS-08 foundations.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-RESEARCH.md
@.gsd/phases/03-dynamic-input-editing/03-02-SUMMARY.md
@src/client/App.tsx
@src/client/lib/api/runpodProxyClient.ts
@src/client/lib/inputEditorStorage.ts
@src/client/lib/workflowStorage.ts
@src/client/features/inputs/useDynamicInputEditor.ts
</context>

<tasks>

<task type="auto">
  <name>Create shared job lifecycle contracts and status helpers</name>
  <files>src/shared/contracts/jobs.ts, tests/shared/jobContracts.test.ts</files>
  <action>Define explicit shared types for recent-job identity, lifecycle status snapshots, terminal reasons, and persisted provenance payloads (saved draft values plus submitted Runpod input). Add status helpers that classify terminal and non-terminal Runpod states using a single source of truth. Include retention constants for visible and total history caps so pruning behavior is configurable from one module. Add tests proving terminal classification and contract invariants expected by client orchestration.</action>
  <verify>Run npm test -- tests/shared/jobContracts.test.ts and confirm terminal classification and contract-shape assertions pass.</verify>
  <done>Shared jobs contracts exist and provide typed lifecycle semantics that downstream polling, cancel, rerun, and history flows can consume without stringly-typed state checks.</done>
</task>

<task type="auto">
  <name>Implement persistent recent-jobs storage with deterministic retention</name>
  <files>src/client/lib/recentJobsStorage.ts, tests/client/recentJobsStorage.test.ts</files>
  <action>Create a Dexie-backed recent-jobs store following existing storage patterns in this repository. Add helpers to upsert submitted records, list visible jobs sorted by submittedAt descending, soft-hide jobs, and prune oldest hidden terminal rows beyond retention caps. Store by jobId primary key with indexes needed for submittedAt and hiddenAt queries. Keep storage API fully typed against shared jobs contracts.</action>
  <verify>Run npm test -- tests/client/recentJobsStorage.test.ts and confirm persistence, sort order, soft-hide behavior, and prune policy pass deterministically.</verify>
  <done>Recent jobs are persisted client-side with bounded growth and query helpers that support current list and future history views.</done>
</task>

<task type="auto">
  <name>Wire run submission to create recent-job records with provenance</name>
  <files>src/client/lib/api/runpodProxyClient.ts, src/client/App.tsx, tests/client/appJobSubmission.test.tsx</files>
  <action>Extend client proxy types so run and status responses can be interpreted for job id and initial status without any casts in UI code. In App run submission flow, capture active template fingerprint and current input draft provenance from the run payload path, then upsert a recent-job record after successful run response. Keep existing run error rendering behavior and ensure failed submissions do not create records. Add client tests proving successful submissions add one recent job and failed submissions add none.</action>
  <verify>Run npm test -- tests/client/appJobSubmission.test.tsx and npm run build.</verify>
  <done>Successful run submissions are persisted as recent-job records with enough metadata for lifecycle tracking and later rerun/load-input actions.</done>
</task>

</tasks>

<verification>
npm test -- tests/shared/jobContracts.test.ts tests/client/recentJobsStorage.test.ts tests/client/appJobSubmission.test.tsx
npm run build
</verification>

<success_criteria>
JOBS-01 foundation is in place: submitting from the current workflow creates a durable recent-job record with typed lifecycle metadata and bounded history retention required by JOBS-08.
</success_criteria>

<output>
After completion, create .gsd/phases/04-job-lifecycle-and-run-management/04-01-SUMMARY.md
</output>
