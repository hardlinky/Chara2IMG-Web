---
phase: 04-job-lifecycle-and-run-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/client/features/jobs/jobStatus.ts
  - src/client/features/jobs/useRecentJobs.ts
  - src/client/lib/api/runpodProxyClient.ts
  - src/client/App.tsx
  - tests/client/jobPolling.test.ts
  - tests/client/useRecentJobs.test.tsx
autonomous: true
must_haves:
  truths:
    - "Each recent job shows an up-to-date lifecycle state that advances until terminal completion, failure, cancellation, or timeout."
    - "Only active jobs are polled, at a fixed 5-second cadence, and polling stops once each job reaches a terminal state or observation timeout."
    - "Temporary polling failures keep the last-known job status visible, surface a warning indicator, and continue retrying on the next poll cycle."
    - "Users can cancel queued or running jobs, and cancelled jobs immediately move to terminal state in recent jobs."
  artifacts:
    - path: "src/client/features/jobs/jobStatus.ts"
      provides: "Central terminal-state and timeout classification helpers for Runpod status snapshots"
    - path: "src/client/features/jobs/useRecentJobs.ts"
      provides: "Single orchestrator hook for recent-job polling, timeout handling, and cancel actions"
    - path: "src/client/lib/api/runpodProxyClient.ts"
      provides: "Cancel proxy client wrapper and typed status payload support"
    - path: "src/client/App.tsx"
      provides: "UI wiring for live status display and cancel-action gating"
    - path: "tests/client/jobPolling.test.ts"
      provides: "Polling stop-condition and timeout regression coverage"
  key_links:
    - from: "src/client/features/jobs/useRecentJobs.ts"
      to: "src/client/lib/api/runpodProxyClient.ts"
      via: "status and cancel proxy calls drive lifecycle transitions"
      pattern: "statusViaProxy|cancelViaProxy"
    - from: "src/client/features/jobs/useRecentJobs.ts"
      to: "src/client/features/jobs/jobStatus.ts"
      via: "classify terminal states and timeout reasons"
      pattern: "isTerminal|classify"
    - from: "src/client/App.tsx"
      to: "src/client/features/jobs/useRecentJobs.ts"
      via: "render recent jobs and trigger cancel only for cancellable states"
      pattern: "useRecentJobs|cancelJob"
---

<objective>
Implement deterministic lifecycle polling, timeout semantics, and cancellation controls over persisted recent jobs.

Purpose: Phase 4 needs reliable status progression and stop conditions so users can monitor and control runs without runaway polling.
Output: Jobs orchestration hook, status/timeout helpers, cancel client wiring, and tests covering JOBS-02, JOBS-03, and JOBS-04.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-RESEARCH.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-01-SUMMARY.md
@src/client/App.tsx
@src/client/lib/api/runpodProxyClient.ts
@src/server/routes/runpodProxy.ts
@src/server/schemas/runpodProxy.ts
</context>

<tasks>

<task type="auto">
  <name>Add lifecycle status classification and timeout helpers</name>
  <files>src/client/features/jobs/jobStatus.ts, tests/client/jobPolling.test.ts</files>
  <action>Create helper functions for terminal-state detection, non-terminal detection, and terminalReason mapping including timed-out and expired-or-not-found outcomes. Include client observation timeout utilities and a fixed 5-second polling cadence constant. Add tests for terminal-state classification, timeout transitions, and known-job 404 classification behavior.</action>
  <verify>Run npm test -- tests/client/jobPolling.test.ts and confirm terminal and timeout classification cases pass.</verify>
  <done>Lifecycle status rules are centralized and test-covered, preventing drift across UI and polling logic.</done>
</task>

<task type="auto">
  <name>Build useRecentJobs orchestrator for polling and cancel flows</name>
  <files>src/client/features/jobs/useRecentJobs.ts, src/client/lib/api/runpodProxyClient.ts, tests/client/useRecentJobs.test.tsx</files>
  <action>Implement a single hook that reads recent jobs from storage, starts polling for non-terminal jobs only, updates snapshots on each status response every 5 seconds, and stops polling on terminal or timeout outcomes. On transient polling failures, preserve the last-known status snapshot, attach a warning marker for the affected row, and retry on the next poll cycle instead of dropping status. Extend the proxy client with cancelViaProxy and typed status response helpers used by the hook. Add cancelJob action that is allowed only for queued or in-progress jobs, performs optimistic cancelling state, calls cancel endpoint, and marks job as CANCELLED terminal on success. Cover hook behavior with tests for start/stop polling, timeout stop, temporary failure warning-and-retry behavior, and cancel gating.</action>
  <verify>Run npm test -- tests/client/useRecentJobs.test.tsx tests/client/jobPolling.test.ts.</verify>
  <done>Recent jobs are continuously and safely tracked through terminal states, and cancel actions are correctly gated and persisted.</done>
</task>

<task type="auto">
  <name>Integrate lifecycle monitor and cancel controls in App</name>
  <files>src/client/App.tsx, tests/client/useRecentJobs.test.tsx</files>
  <action>Wire App to consume useRecentJobs and render a recent-jobs monitor section showing full job id, submitted time, and status as badge plus text for each row. Keep the existing run submission path but hand off post-submit tracking to hook APIs introduced in this plan. Add cancel controls only for cancellable states and disable action buttons during in-flight cancel operations. Ensure run errors and lifecycle errors remain separated in UI messaging, and expose temporary polling warning state without clearing the last-known status.</action>
  <verify>Run npm test -- tests/client/useRecentJobs.test.tsx and npm run build.</verify>
  <done>Users can observe live lifecycle status per submitted job and cancel queued/running jobs from the app surface.</done>
</task>

</tasks>

<verification>
npm test -- tests/client/jobPolling.test.ts tests/client/useRecentJobs.test.tsx
npm run build
</verification>

<success_criteria>
JOBS-02 through JOBS-04 are satisfied: each job status updates until terminal, only active jobs are polled every 5 seconds with warning-and-retry on temporary failures, and cancel actions work only for valid lifecycle states.
</success_criteria>

<output>
After completion, create .gsd/phases/04-job-lifecycle-and-run-management/04-02-SUMMARY.md
</output>
