---
phase: 04-job-lifecycle-and-run-management
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/client/features/jobs/RecentJobsPanel.tsx
  - src/client/features/jobs/useRecentJobs.ts
  - src/client/features/inputs/useDynamicInputEditor.ts
  - src/client/lib/api/runpodProxyClient.ts
  - src/client/lib/recentJobsStorage.ts
  - src/client/App.tsx
  - tests/client/useRecentJobs.test.tsx
  - tests/client/dynamicInputEditor.test.tsx
  - tests/client/recentJobsPanel.test.tsx
autonomous: true
must_haves:
  truths:
    - "Users can rerun a previous job as a new submission using that job's saved workflow input provenance."
    - "Users can load a previous job's saved input draft back into the Input tab only when the workflow template fingerprint matches."
    - "Users see a compact newest-first mixed list (no split sections) with full job ID, status badge plus text, and relative submitted time with absolute timestamp on hover."
    - "The list includes a status filter dropdown near the list title (default All) and bottom pagination with numbered pages plus Prev/Next; filter persists across refresh while page resets to 1."
    - "If a new job is submitted while viewing page 2+, the list auto-jumps to page 1 so the newest row is immediately visible."
    - "Users can remove a job from the visible list as a one-way action with no restore UI; removed jobs are retained internally for 24 hours then auto-deleted."
    - "Completed rows show execution time when available, and failed/cancelled/timed-out rows show a brief failure reason snippet."
    - "Empty list state renders exactly: No recent jobs yet"
  artifacts:
    - path: "src/client/features/jobs/useRecentJobs.ts"
      provides: "Rerun, load-input metadata access, remove-visible one-way action, and filtered/paginated visible list state"
    - path: "src/client/features/jobs/RecentJobsPanel.tsx"
      provides: "Recent jobs UI with compact rows, status filter, pagination, and row actions"
    - path: "src/client/features/inputs/useDynamicInputEditor.ts"
      provides: "Explicit API for applying externally provided draft values into active input editor state"
    - path: "src/client/lib/recentJobsStorage.ts"
      provides: "Visible-list query, one-way soft-hide, and 24-hour cleanup helpers for removed rows"
  key_links:
    - from: "src/client/features/jobs/RecentJobsPanel.tsx"
      to: "src/client/features/jobs/useRecentJobs.ts"
      via: "UI actions invoke rerun, load inputs, remove-visible, filter updates, and pagination"
      pattern: "rerunJob|loadJobInputs|removeVisibleJob|setStatusFilter|setPage"
    - from: "src/client/App.tsx"
      to: "src/client/features/inputs/useDynamicInputEditor.ts"
      via: "load prior inputs action applies saved draft into current editor"
      pattern: "applyExternalDraft|loadInputs"
    - from: "src/client/features/jobs/useRecentJobs.ts"
      to: "src/client/lib/recentJobsStorage.ts"
      via: "one-way soft hide plus 24-hour cleanup and filtered/paged visible listing"
      pattern: "hideRecentJob|listVisibleRecentJobs|pruneRemovedJobs"
---

<objective>
Deliver rerun, load-inputs, one-way remove-visible, and final recent-jobs list UX requirements on top of lifecycle tracking.

Purpose: Phase 4 completes iteration workflows by letting users return to prior runs without losing workflow context.
Output: Jobs panel UX, rerun/load/remove orchestration, filter/pagination behavior, and retention semantics that satisfy JOBS-05 through JOBS-08.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-RESEARCH.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-02-SUMMARY.md
@src/client/App.tsx
@src/client/features/inputs/useDynamicInputEditor.ts
@src/client/lib/inputEditorStorage.ts
@src/client/lib/recentJobsStorage.ts
@src/client/lib/api/runpodProxyClient.ts
</context>

<tasks>

<task type="auto">
  <name>Extend recent-jobs orchestration with rerun, one-way remove, filtering, and pagination APIs</name>
  <files>src/client/features/jobs/useRecentJobs.ts, src/client/lib/recentJobsStorage.ts, src/client/lib/api/runpodProxyClient.ts, tests/client/useRecentJobs.test.tsx</files>
  <action>Add hook actions for rerun-as-new-job from saved payload input and one-way remove-visible via soft-hide persistence. Add status-filter state (default All), paginated visible-list state, and filter persistence across refresh while always resetting page index to 1 on reload. Ensure that if a new job is submitted while current page is greater than 1, the hook resets to page 1 automatically. Enforce action gating by lifecycle state so rerun/remove cannot execute in invalid states. Keep 24-hour cleanup behavior for removed rows and avoid any restore/unhide APIs in this phase. Add tests covering rerun creation, remove one-way behavior, filter persistence + page reset, and auto-jump-to-page-1 on new submission from page 2+.</action>
  <verify>Run npm test -- tests/client/useRecentJobs.test.tsx and confirm rerun creates a new job row, remove is one-way soft hide, and filter/pagination state rules pass.</verify>
  <done>Recent-jobs orchestration supports rerun and one-way remove flows plus deterministic filter/pagination behavior aligned to Phase 4 UX decisions.</done>
</task>

<task type="auto">
  <name>Add load-prior-inputs bridge with template-fingerprint safety</name>
  <files>src/client/features/inputs/useDynamicInputEditor.ts, src/client/App.tsx, tests/client/dynamicInputEditor.test.tsx</files>
  <action>Expose an explicit editor API that can replace current draft values from an external source while preserving inline validation semantics and persistence behavior. In App, wire load inputs action from selected recent job to the editor API, but only apply when recent-job template fingerprint matches the active template fingerprint. Show a clear non-blocking mismatch message and skip apply when fingerprints differ. Add tests proving matched fingerprints apply values and mismatched fingerprints are blocked.</action>
  <verify>Run npm test -- tests/client/dynamicInputEditor.test.tsx and npm run build.</verify>
  <done>Users can reload saved inputs from prior runs into the Input tab safely without corrupting drafts across different workflow templates.</done>
</task>

<task type="auto">
  <name>Build RecentJobsPanel UX and connect actions end to end</name>
  <files>src/client/features/jobs/RecentJobsPanel.tsx, src/client/App.tsx, tests/client/recentJobsPanel.test.tsx</files>
  <action>Create a dedicated RecentJobsPanel component that renders only visible jobs in compact newest-first mixed rows (no split active/finished sections and no expandable details). Each row must show full job ID, colored status badge plus text, relative submitted time with absolute hover timestamp, execution time for completed rows when available, and brief failure reason snippets for failed/cancelled/timed-out rows. Render status filter dropdown near the list title with default All. Render bottom pagination with numbered pages plus Prev/Next. Keep empty state text exactly "No recent jobs yet". Expose actions for rerun, load inputs, and remove from visible list; do not add history toggle or restore UI. Wire panel callbacks to useRecentJobs and editor-bridge actions. Add panel tests covering required row content, exact empty-state text, filter + pagination behavior, and absence of history/restore controls.</action>
  <verify>Run npm test -- tests/client/recentJobsPanel.test.tsx tests/client/useRecentJobs.test.tsx and npm run build.</verify>
  <done>Phase 4 UX supports rerun, load-inputs, and one-way remove-visible flows with the required compact list, filter, pagination, and row-detail behaviors.</done>
</task>

</tasks>

<verification>
npm test -- tests/client/useRecentJobs.test.tsx tests/client/dynamicInputEditor.test.tsx tests/client/recentJobsPanel.test.tsx
npm run build
</verification>

<success_criteria>
JOBS-05 through JOBS-08 are complete: users can rerun prior jobs, reload prior inputs into the input editor with fingerprint safety, remove jobs from the visible list one-way, and manage recent runs through the required mixed list, status filter, pagination, and 24-hour removed-row retention policy.
</success_criteria>

<output>
After completion, create .gsd/phases/04-job-lifecycle-and-run-management/04-03-SUMMARY.md
</output>
