---
phase: 05-outputs-and-gallery-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/jobs.ts
  - src/client/lib/jobSubmission.ts
  - src/client/lib/runpodOutputImage.ts
  - src/client/lib/jobOutputProjection.ts
  - src/client/features/jobs/useRecentJobs.ts
  - src/client/App.tsx
  - tests/client/appJobSubmission.test.tsx
  - tests/client/runpodOutputImage.test.ts
  - tests/client/jobOutputProjection.test.ts
autonomous: true
must_haves:
  truths:
    - "Completed jobs with image outputs can be transformed into a deterministic output asset list without parsing payloads in UI components."
    - "Multi-image Runpod responses produce all generated images in stable order, not just the first match."
    - "Each output asset remains associated with the source job and includes provenance sufficient for gallery and dedicated output review."
    - "Workflow filename provenance is captured at submission time for new jobs and safely handled for older jobs where filename is missing."
  artifacts:
    - path: "src/client/lib/jobOutputProjection.ts"
      provides: "Projection helpers that map RecentJobRecord values into gallery-ready per-job output assets with provenance"
    - path: "src/client/lib/runpodOutputImage.ts"
      provides: "Full-output image extraction utility returning ordered image candidates from nested Runpod responses"
    - path: "src/shared/contracts/jobs.ts"
      provides: "Recent-job provenance contract extended with workflow filename and output-ready typing"
    - path: "src/client/lib/jobSubmission.ts"
      provides: "Submission persistence that snapshots workflow filename into job provenance"
  key_links:
    - from: "src/client/App.tsx"
      to: "src/client/lib/jobSubmission.ts"
      via: "Submission snapshot includes active workflow display name as provenance"
      pattern: "displayName|workflowFileName|submitRunAndPersistRecentJob"
    - from: "src/client/lib/jobSubmission.ts"
      to: "src/shared/contracts/jobs.ts"
      via: "Persisted RecentJobSubmissionInput includes provenance workflow filename"
      pattern: "RecentJobSubmissionInput|provenance"
    - from: "src/client/lib/jobOutputProjection.ts"
      to: "src/client/lib/runpodOutputImage.ts"
      via: "Projection uses full-output extraction to build per-job asset arrays"
      pattern: "extractRunpod.*Images|project.*Output"
---

<objective>
Establish output data foundations by capturing required provenance and projecting completed-job responses into deterministic multi-image output assets.

Purpose: Phase 5 gallery behaviors depend on trustworthy output extraction and job provenance wiring before UI implementation.
Output: Extended contracts, submission provenance capture, reusable output projection utilities, and fixture-driven tests proving multi-image and provenance correctness.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/05-outputs-and-gallery-review/05-CONTEXT.md
@.gsd/phases/05-outputs-and-gallery-review/05-RESEARCH.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-01-SUMMARY.md
@.gsd/phases/04-job-lifecycle-and-run-management/04-03-SUMMARY.md
@src/shared/contracts/jobs.ts
@src/client/lib/jobSubmission.ts
@src/client/lib/runpodOutputImage.ts
@src/client/lib/recentJobsStorage.ts
@tests/client/fixtures/workflows/comfyui-valid-template.json
</context>

<tasks>

<task type="auto">
  <name>Extend recent-job provenance contracts to include workflow filename and output-review fields</name>
  <files>src/shared/contracts/jobs.ts, src/client/lib/jobSubmission.ts, src/client/App.tsx, tests/client/appJobSubmission.test.tsx</files>
  <action>Add workflow filename (imported workflow display name) to recent-job provenance so output review can show full provenance in dedicated view. Update submission snapshot and persistence wiring so each newly submitted job records this field at submission time. Preserve backward compatibility for legacy persisted jobs by keeping this field optional at read boundaries, while requiring it on new writes. Do not invent workflow names from internal node metadata; use imported workflow filename only. Add or update tests proving saved recent jobs now include workflow filename provenance and legacy records without filename remain readable.</action>
  <verify>Run npm test -- tests/client/appJobSubmission.test.tsx and confirm provenance assertions pass for both new and legacy records.</verify>
  <done>Recent-job contracts and submission flow persist workflow filename provenance required by OUTP-03 and dedicated provenance display.</done>
</task>

<task type="auto">
  <name>Replace single-image preview extraction with full multi-image extraction support</name>
  <files>src/client/lib/runpodOutputImage.ts, tests/client/runpodOutputImage.test.ts</files>
  <action>Introduce a new extractor that walks terminal Runpod response payloads and returns all valid image outputs in stable source order, including nested arrays/objects and data URL or raw base64 forms already supported today. Keep MIME detection and validation safeguards from the current helper. Keep or wrap the existing single-preview API only as a compatibility shim if needed, but make the new plural extractor the canonical utility for gallery use. Add tests using realistic nested payload fixtures to prove multiple images are found, ordering is deterministic, invalid payloads are ignored, and unsupported media is excluded.</action>
  <verify>Run npm test -- tests/client/runpodOutputImage.test.ts and confirm multi-image extraction coverage passes with deterministic ordering expectations.</verify>
  <done>Output extraction returns complete per-job image sets so multi-image jobs can be represented accurately in gallery and lightbox flows.</done>
</task>

<task type="auto">
  <name>Create output projection layer from persisted jobs to gallery-ready output assets</name>
  <files>src/client/lib/jobOutputProjection.ts, src/client/features/jobs/useRecentJobs.ts, tests/client/jobOutputProjection.test.ts</files>
  <action>Create a projection module that maps RecentJobRecord entries into a normalized JobOutputAsset and per-job cluster shape containing job ID, relative completion metadata inputs, workflow filename provenance, representative first image, output count, and ordered image list. Omit jobs that are failed, cancelled, timed out, still active, or terminal with no image outputs. Preserve newest-first ordering by job completion/submission timestamp. Expose the projection through existing jobs orchestration in a way Phase 5 UI can consume without parsing raw response payloads. Add fixture-driven tests covering multi-image job projection, representative selection, omitted non-output jobs, and provenance passthrough behavior.</action>
  <verify>Run npm test -- tests/client/jobOutputProjection.test.ts and npm run build.</verify>
  <done>A stable output projection contract exists and is verified, enabling Phase 5 UI implementation without payload-coupled rendering logic.</done>
</task>

</tasks>

<verification>
npm test -- tests/client/appJobSubmission.test.tsx tests/client/runpodOutputImage.test.ts tests/client/jobOutputProjection.test.ts
npm run build
</verification>

<success_criteria>
OUTP-02 and OUTP-03 foundations are complete: the system can derive all images for completed jobs, preserve source-job linkage, and include workflow filename provenance for dedicated review surfaces.
</success_criteria>

<output>
After completion, create .gsd/phases/05-outputs-and-gallery-review/05-01-SUMMARY.md
</output>
