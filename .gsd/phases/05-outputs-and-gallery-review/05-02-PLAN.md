---
phase: 05-outputs-and-gallery-review
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - src/client/App.tsx
  - src/client/features/outputs/useOutputGallery.ts
  - src/client/features/outputs/OutputsTab.tsx
  - src/client/features/outputs/JobOutputsView.tsx
  - src/client/features/outputs/OutputLightbox.tsx
  - src/client/features/outputs/outputsGallery.css
  - tests/client/outputGallery.test.tsx
  - tests/client/outputLightbox.test.tsx
  - tests/client/appOutputsTab.test.tsx
autonomous: true
must_haves:
  truths:
    - "Users can open an Outputs tab and browse images produced by completed jobs in a gallery-oriented flow."
    - "Gallery shows newest jobs first with subtle per-job separators and collapsed cards (including single-image jobs) using representative first image plus count badge."
    - "Expanding a job opens a dedicated job-output view and returning restores exact prior gallery context (scroll position, density, selected cluster)."
    - "Lightbox opens from image click with keyboard and touch navigation, fit-to-viewport default, wrap navigation within current job, and explicit close/back exit."
    - "Gallery and dedicated view expose required provenance at the correct density: minimal Job ID in gallery and full compact provenance line in dedicated view."
  artifacts:
    - path: "src/client/features/outputs/OutputsTab.tsx"
      provides: "Outputs tab gallery shell with masonry layout, density control, lazy loading, and job cluster rendering"
    - path: "src/client/features/outputs/JobOutputsView.tsx"
      provides: "Dedicated per-job output view with provenance line, paginated/lazy rendering, and return-state restoration"
    - path: "src/client/features/outputs/OutputLightbox.tsx"
      provides: "PhotoSwipe lightbox integration scoped to current job outputs with zoom, wrap, and control behavior"
    - path: "src/client/features/outputs/useOutputGallery.ts"
      provides: "State orchestration for cluster expansion, return context snapshot/restore, and gallery data flow"
  key_links:
    - from: "src/client/features/outputs/useOutputGallery.ts"
      to: "src/client/features/jobs/useRecentJobs.ts"
      via: "Consumes projected output clusters rather than raw Runpod payloads"
      pattern: "project|outputCluster|completedOutputs"
    - from: "src/client/features/outputs/OutputsTab.tsx"
      to: "src/client/features/outputs/JobOutputsView.tsx"
      via: "Expand action transitions into dedicated job view with saved return context"
      pattern: "openJobOutputs|restore|scroll"
    - from: "src/client/features/outputs/OutputLightbox.tsx"
      to: "src/client/features/outputs/JobOutputsView.tsx"
      via: "Lightbox receives only current job images and opens at selected index"
      pattern: "PhotoSwipe|loop|allowPanToNext|preload"
---

<objective>
Implement the Outputs tab browsing experience with masonry gallery, dedicated per-job output view, and job-scoped lightbox interactions that preserve provenance and return context.

Purpose: Complete Phase 5 user-facing output review behavior so completed jobs can be reviewed through an intentional gallery flow instead of raw payload inspection.
Output: Outputs tab UI, gallery state orchestration, dedicated job-output view, PhotoSwipe-based lightbox behavior, and tests validating locked interaction semantics.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/05-outputs-and-gallery-review/05-CONTEXT.md
@.gsd/phases/05-outputs-and-gallery-review/05-RESEARCH.md
@.gsd/phases/05-outputs-and-gallery-review/05-01-SUMMARY.md
@src/client/App.tsx
@src/client/features/jobs/useRecentJobs.ts
@src/client/lib/jobOutputProjection.ts
@tests/client/recentJobsPanel.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Create outputs gallery state orchestration and install lightbox dependencies</name>
  <files>package.json, src/client/features/outputs/useOutputGallery.ts, tests/client/appOutputsTab.test.tsx</files>
  <action>Install photoswipe and react-photoswipe-gallery. Add output-gallery orchestration hook that consumes projected output clusters from Phase 5 plan 01, enforces newest-first ordering, and owns session-only density state (compact/balanced/comfortable) with balanced as default and no persistent storage. Implement return-context snapshot/restore (scroll position, selected cluster, density) when navigating between gallery and dedicated job view. Ensure failed or empty-output jobs are excluded before rendering and that collapsed-card pattern applies consistently even when a job has one image.</action>
  <verify>Run npm test -- tests/client/appOutputsTab.test.tsx and npm ls photoswipe react-photoswipe-gallery.</verify>
  <done>Outputs flow has deterministic state orchestration and required dependencies for dedicated view and lightbox behaviors.</done>
</task>

<task type="auto">
  <name>Build Outputs tab gallery and dedicated per-job output view with provenance rules</name>
  <files>src/client/App.tsx, src/client/features/outputs/OutputsTab.tsx, src/client/features/outputs/JobOutputsView.tsx, src/client/features/outputs/outputsGallery.css, tests/client/outputGallery.test.tsx, tests/client/appOutputsTab.test.tsx</files>
  <action>Add an Outputs tab entry in the app shell and render a masonry-style gallery using CSS multi-column plus break-inside avoidance to preserve image aspect ratio and balanced spacing. Apply responsive columns (1 on small phones, 2 on larger phones, wider multi-column behavior on desktop). Render image-only tiles with subtle per-job separators and minimal provenance marker (Job ID only). For each job cluster, show representative first image with output count badge in collapsed state; expanding opens a dedicated per-job view showing full provenance line (Job ID, relative completion time, workflow filename). In dedicated view, lazy-load or paginate large output sets, include optional jump-to-next-job action, and restore exact prior gallery state and scroll on return.</action>
  <verify>Run npm test -- tests/client/outputGallery.test.tsx tests/client/appOutputsTab.test.tsx and npm run build.</verify>
  <done>OUTP-01 and OUTP-04 are satisfied with locked gallery semantics, responsive masonry behavior, and dedicated view return-context restoration.</done>
</task>

<task type="auto">
  <name>Integrate PhotoSwipe lightbox with job-scoped navigation and zoom behavior</name>
  <files>src/client/features/outputs/OutputLightbox.tsx, src/client/features/outputs/OutputsTab.tsx, src/client/features/outputs/JobOutputsView.tsx, tests/client/outputLightbox.test.tsx</files>
  <action>Implement lightbox opening on image click using react-photoswipe-gallery + PhotoSwipe, scoped by default to the current job outputs only. Configure fit-to-viewport start, keyboard arrows, touch swipe, wrap-at-ends navigation, and explicit close/back exit behavior. Configure zoom and pan precedence so pan dominates while zoomed (for example via allowPanToNext false), with swipe navigation active at fit zoom. Enable chrome auto-hide/reveal behavior and preload the full current job output list (or a performant equivalent that preserves smooth next/previous interaction for large images). Keep implementation extensible for future large-image tuning without changing user-visible semantics.</action>
  <verify>Run npm test -- tests/client/outputLightbox.test.tsx tests/client/outputGallery.test.tsx and npm run build.</verify>
  <done>Lightbox behavior matches locked interaction decisions, including navigation scope, zoom/pan semantics, explicit exits, and wrap behavior.</done>
</task>

</tasks>

<verification>
npm test -- tests/client/appOutputsTab.test.tsx tests/client/outputGallery.test.tsx tests/client/outputLightbox.test.tsx
npm run build
</verification>

<success_criteria>
OUTP-01 through OUTP-04 are complete: users can open Outputs, browse completed-job images in a gallery flow, review multi-image jobs in dedicated view and lightbox, and retain clear job provenance throughout.
</success_criteria>

<output>
After completion, create .gsd/phases/05-outputs-and-gallery-review/05-02-SUMMARY.md
</output>
