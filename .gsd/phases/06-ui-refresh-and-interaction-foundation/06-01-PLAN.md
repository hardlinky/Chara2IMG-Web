---
phase: 06-ui-refresh-and-interaction-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/styles/tokens.css
  - src/client/styles/base.css
  - src/client/styles/layout.css
  - src/client/styles/components.css
  - src/client/styles/index.css
  - src/client/main.tsx
  - tests/client/uiThemeFoundation.test.tsx
autonomous: true
must_haves:
  truths:
    - "Theme 01 dark studio visual tokens drive shared surfaces, typography, and color semantics across tabs."
    - "Primary, secondary, and destructive action styles are globally distinct before tab-specific styling begins."
    - "Keyboard focus-visible, hover, active, disabled, and error visual states are available as reusable control styles."
    - "Motion remains minimal and respects reduced-motion preferences."
  artifacts:
    - path: "src/client/styles/tokens.css"
      provides: "Semantic token layer for Theme 01 (surface/text/accent/state/radius/shadow)"
    - path: "src/client/styles/components.css"
      provides: "Reusable interaction styles for buttons, form controls, cards, status, and state modifiers"
    - path: "src/client/styles/index.css"
      provides: "Single style entrypoint wiring token, base, layout, and component layers"
    - path: "src/client/main.tsx"
      provides: "Global stylesheet import so all tabs inherit theme foundation"
  key_links:
    - from: "src/client/main.tsx"
      to: "src/client/styles/index.css"
      via: "Global stylesheet import"
      pattern: 'import\s+"./styles/index.css"'
    - from: "src/client/styles/components.css"
      to: "src/client/styles/tokens.css"
      via: "Component styles consume semantic tokens instead of raw hex values"
      pattern: 'var\(--'
    - from: "src/client/styles/components.css"
      to: "interactive controls"
      via: "focus-visible and reduced-motion selectors"
      pattern: "focus-visible|prefers-reduced-motion"
---

<objective>
Establish the Theme 01 design-system foundation as a shared global style layer before touching tab-specific UI composition.

Purpose: Phase 6 needs centralized tokens and interaction primitives to avoid one-off tab styling drift and preserve multi-theme extensibility.
Output: Tokenized global CSS foundation, reusable stateful component styles, and baseline tests for token/state availability.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/06-ui-refresh-and-interaction-foundation/06-CONTEXT.md
@.gsd/phases/06-ui-refresh-and-interaction-foundation/06-RESEARCH.md
@.gsd/phases/05-outputs-and-gallery-review/05-02-SUMMARY.md
@src/client/main.tsx
@src/client/features/outputs/outputsGallery.css
</context>

<tasks>

<task type="auto">
  <name>Create semantic Theme 01 token layer and base typography/surface styles</name>
  <files>src/client/styles/tokens.css, src/client/styles/base.css, src/client/styles/index.css, src/client/main.tsx</files>
  <action>Create a token-first stylesheet architecture for Theme 01 using semantic variables (surface/text/accent/state/border/shadow/radius/spacing) with a data-theme root contract that defaults to theme-01. Implement creative-studio dark surfaces with burnt-orange/copper accent values, balanced contrast, subtle gradients, and modern neutral sans fallback stack. Wire all styles through one index stylesheet imported by main.tsx. Avoid hard-coding palette values in component files after this change; expose tokens intended for later theme swaps.</action>
  <verify>Run npm run build and confirm main entry imports the shared stylesheet without TypeScript or bundling regressions.</verify>
  <done>Theme 01 tokens and base global surfaces/typography are centralized and loaded app-wide.</done>
</task>

<task type="auto">
  <name>Implement reusable interaction-state primitives for controls, cards, and feedback surfaces</name>
  <files>src/client/styles/layout.css, src/client/styles/components.css</files>
  <action>Add reusable class patterns for cards, section surfaces, segmented pills, button hierarchy (primary/secondary/destructive), form controls, inline status blocks, and toast container shells. Include explicit focus-visible, hover, active, disabled, and error states for key controls using token aliases. Add reduced-motion guardrails so transitions are minimal and disabled when prefers-reduced-motion is set. Keep radii in the slight 6-8px range and avoid heavy animation.</action>
  <verify>Run npm run build and inspect generated CSS for focus-visible and prefers-reduced-motion selectors.</verify>
  <done>Global primitives exist for stateful controls and visual hierarchy, satisfying UI-02 and UI-04 foundations.</done>
</task>

<task type="auto">
  <name>Add baseline tests for theme and interaction-state wiring</name>
  <files>tests/client/uiThemeFoundation.test.tsx</files>
  <action>Add a lightweight test suite that verifies the global style entry import exists and that required class/token contracts for primary/secondary/destructive and focus/error states are present in style sources. Keep the test implementation deterministic (string/selector assertions) and avoid snapshot brittleness.</action>
  <verify>Run npm test -- tests/client/uiThemeFoundation.test.tsx.</verify>
  <done>Automated checks protect Theme 01 token/state contracts from accidental removal in later plans.</done>
</task>

</tasks>

<verification>
npm test -- tests/client/uiThemeFoundation.test.tsx
npm run build
</verification>

<success_criteria>
UI-01/UI-02/UI-04 foundations are in place: Theme 01 semantic tokens and reusable interaction states are globally available and tested.
</success_criteria>

<output>
After completion, create .gsd/phases/06-ui-refresh-and-interaction-foundation/06-01-SUMMARY.md
</output>
