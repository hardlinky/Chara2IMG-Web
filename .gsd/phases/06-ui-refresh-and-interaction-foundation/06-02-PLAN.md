---
phase: 06-ui-refresh-and-interaction-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/client/components/app-shell/AppShell.tsx
  - src/client/components/app-shell/TopTabRail.tsx
  - src/client/components/app-shell/BottomTabNav.tsx
  - src/client/App.tsx
  - tests/client/appShellNavigation.test.tsx
autonomous: true
must_haves:
  truths:
    - "Users navigate Setup/Input/Jobs/Output through an accessible segmented rail on desktop/tablet and a phone bottom nav on small screens."
    - "Active tab state is visually obvious and programmatically exposed through ARIA tab semantics."
    - "App shell presents a two-tier header with global status on row one and contextual controls on row two."
    - "Tab switching preserves feature behavior from Phases 1-5 without regression."
  artifacts:
    - path: "src/client/components/app-shell/AppShell.tsx"
      provides: "Two-tier shell layout and tab panel hosting"
    - path: "src/client/components/app-shell/TopTabRail.tsx"
      provides: "Floating segmented top tab rail with ARIA tablist semantics"
    - path: "src/client/components/app-shell/BottomTabNav.tsx"
      provides: "Phone bottom navigation mirroring four tab destinations"
    - path: "src/client/App.tsx"
      provides: "Business orchestration wired into shell slots without feature logic regressions"
  key_links:
    - from: "src/client/App.tsx"
      to: "src/client/components/app-shell/AppShell.tsx"
      via: "App delegates tab chrome and panel framing to shell"
      pattern: "AppShell|activeTab"
    - from: "src/client/components/app-shell/TopTabRail.tsx"
      to: "tab content sections"
      via: "role tab with aria-controls and roving tab index"
      pattern: 'role="tab"|aria-controls|aria-selected|tabIndex'
    - from: "src/client/components/app-shell/BottomTabNav.tsx"
      to: "tab state"
      via: "Same destination IDs and active-state synchronization"
      pattern: "setup|input|jobs|output"
---

<objective>
Introduce a reusable app shell that delivers locked navigation and hierarchy decisions while preserving existing Setup/Input/Jobs/Output business behavior.

Purpose: Phase 6 requires structural consistency first so tab-specific visual work can proceed independently without duplicating shell logic.
Output: Two-tier shell components, accessible segmented tab navigation, mobile bottom nav, and shell-level tests.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/06-ui-refresh-and-interaction-foundation/06-CONTEXT.md
@.gsd/phases/06-ui-refresh-and-interaction-foundation/06-RESEARCH.md
@.gsd/phases/06-ui-refresh-and-interaction-foundation/06-01-SUMMARY.md
@src/client/App.tsx
@src/client/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Create app shell components with two-tier header and segmented tab rail semantics</name>
  <files>src/client/components/app-shell/AppShell.tsx, src/client/components/app-shell/TopTabRail.tsx, src/client/components/app-shell/BottomTabNav.tsx</files>
  <action>Create shell components that render: row-1 header area (title/version/global status), row-2 contextual controls region, top segmented tab rail for desktop/tablet, and phone bottom nav for the same four destinations. Implement ARIA tabs semantics (tablist/tab/tabpanel, aria-selected, aria-controls, keyboard-friendly tabIndex behavior). Use outline icon state for inactive tabs and filled icon state for active tabs, with active full-pill treatment and muted outline pills for inactive tabs.</action>
  <verify>Run npm run build and ensure shell components compile with strict typing and no missing prop wiring.</verify>
  <done>Shell primitives exist for locked layout/navigation decisions with accessible tab semantics.</done>
</task>

<task type="auto">
  <name>Refactor App orchestration into shell slots while preserving Phase 1-5 feature behavior</name>
  <files>src/client/App.tsx</files>
  <action>Replace current inline tab button strip with AppShell composition. Keep existing feature orchestration and state machines (invite gate, key handling, endpoint loading, workflow import/editing, jobs actions, outputs view) unchanged in behavior. Route current tab content into explicit tab panels and map contextual controls into header row 2 where applicable. Preserve app version labeling and current tab defaults.</action>
  <verify>Run npm test -- tests/client/appJobSubmission.test.tsx tests/client/recentJobsPanel.test.tsx tests/client/outputGallery.test.tsx.</verify>
  <done>App now renders through shared shell with no regressions in existing critical flows.</done>
</task>

<task type="auto">
  <name>Add shell navigation tests for ARIA and active-state behavior</name>
  <files>tests/client/appShellNavigation.test.tsx</files>
  <action>Add tests confirming: exactly four tab destinations exist, selected tab exposes aria-selected true and correct tabIndex, panel relationships are wired via aria-controls/aria-labelledby, and mobile nav mirrors the same destination IDs. Keep tests stable by asserting semantic attributes and text labels rather than implementation-specific class names.</action>
  <verify>Run npm test -- tests/client/appShellNavigation.test.tsx.</verify>
  <done>Shell navigation semantics are protected against accessibility or wiring regressions.</done>
</task>

</tasks>

<verification>
npm test -- tests/client/appShellNavigation.test.tsx tests/client/appJobSubmission.test.tsx tests/client/recentJobsPanel.test.tsx tests/client/outputGallery.test.tsx
npm run build
</verification>

<success_criteria>
UI-01 and UI-03 structure is established: coherent shell hierarchy and adaptive navigation are implemented with accessible tab semantics.
</success_criteria>

<output>
After completion, create .gsd/phases/06-ui-refresh-and-interaction-foundation/06-02-SUMMARY.md
</output>
